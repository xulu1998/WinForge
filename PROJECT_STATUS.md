# Project Status

> Authoritative live status of the WinForge project. Every Agent must update
> this file after completing a task.

## Summary

| Field | Value |
|-------|-------|
| Project | WinForge |
| Version | 0.1.0-alpha |
| Current Phase | Phase 16 — Full Health Validation & Release Confidence: **PHASE 17 — IN PROGRESS · RELEASE-CANDIDATE HARDENING IMPLEMENTED (2026-08-16; branch `phase/17-release-candidate-hardening` from main 59355b9, NOT merged). BALANCED — FullHealthValidated · DEDICATEDGAMING — FullHealthValidated · OTHER PROFILES — TRUTHFUL LOWER VALIDATION LEVELS (Gaming/Developer/Office/Lightweight = WorkflowValidated).** FIRST REAL BALANCED VM VALIDATION SUCCEEDED through the desktop (ISO booted in VMware, Windows 11 Pro 25H2 build 26200 installed, OOBE completed; commit evidence profile-commit-validation.json: 16 BuildPlan ops/10 selected/10 executed/10 read-back Verified, committed+postCommitVerified true, ISO structure validated, cleanup discard+workspace succeeded). The first full-health-report.json failed ONLY on three health-check LOGIC defects now fixed (ADR-098 addendum): (1) sfcVerifyOnly falsely failed a successful zh-CN run — root cause: English-only text match + native UTF-16LE output (low NUL ratio on Chinese Windows) mis-decoded; FIX: EXIT-CODE authoritative verdict (0 = no integrity violations, locale-independent) + localized success marker corroboration only, file-redirected capture + candidate-scoring decoder (BOM → UTF-8 → UTF-16LE → ANSI, fewest U+FFFD wins) + NUL stripping — a successful run can never fail on capture artifacts; genuine failures still Fail; /scannow never automatic. (2)+(3) defaultUser_Start_* falsely failed — OOBE legitimately consumes the seeded Default-User template into the created user's profile (HKCU Start_ShowRecommended/Start_ShowRecent = 0x0, manual-verified); FIX: expected registry checks declare EXPLICIT scope — OfflineMachine (HKLM) / CurrentUserEffective (HKCU, for profile-seeding values) / DefaultUserTemplate (Users\Default\NTUSER.DAT); missing/unknown scope rejects the file; Balanced Start values = CurrentUserEffective, 4 machine policies = OfflineMachine; image-time WIM Default-User validation UNCHANGED. Mojibake "鈥?" in the report fixed: script now pure-ASCII + UTF-8 BOM (PS 5.1 ANSI-parsing was the source); JSON round-trips Chinese Unicode. FullHealthValidated gate corrected: Warnings (incl. HTTPS TLS-trust with IP/DNS Pass) do NOT block — only Fail checks and untested critical sections block. Windows identity display normalized to "Windows 11" (legacy "Windows 10 Pro" ProductName + build >= 22000) — presentation only. SECOND real Balanced report is ZERO-failure (failures=[]; all sections Pass except windowsIdentity activation Warning report-only + network HTTPS TLS-trust Warning with IP/DNS Pass; servicing CheckHealth+SFC Pass). STAGE 16.1b (ADR-098 addendum): the remaining false-negative was the GATE - the OPTIONAL DISM /ScanHealth was treated as required; every check now declares requiredForFullHealth (JSON, omitted=true): REQUIRED checks (CheckHealth, SFC, IP/DNS, edition/build/arch/lang/boot, ...) must be tested (NotTested blocks; failures=[] alone insufficient), OPTIONAL checks (ScanHealth, HTTPS, activation, Defender signatures, audio) may be NotTested/Warning without blocking, any Fail blocks conservatively; section status derives from required checks, overallStatus stays the honest worst (optional NotTested excluded), the gate is required-only. Expected corrected result: overallStatus Warning / failures [] / fullHealthValidated true. **Balanced formally FullHealthValidated** (real Win11 Pro 25H2 zh-CN x64 build 26200.8037, VMware Workstation Pro: failures=[], overallStatus Warning, fullHealthValidated=true; required gates all Pass — identity / boot-shell / devices / DHCP-IP / DNS / DISM CheckHealth / SFC / Windows Update / Security / Defender / Firewall / Store / Balanced expected-state; allowed non-blockers: activation Notification + HTTPS TLS-trust Warning + optional ScanHealth NotTested). **16.2 COMPLETE — DedicatedGaming FullHealthValidated**: real commit evidence profile-commit-validation.json (33 BuildPlan / 20 selected / 20 executed + read-back Verified; preCommitValidationPassed=true, committed=true, postCommitVerified=true, committedImageReadable=true, iso.structureValidated=true, 20 post-commit checks, cleanup discard+workspace succeeded); ISO Documents\WinForge\WinForge-DedicatedGaming-Win11-25H2-Pro-zh-CN-x64.iso (8,052,092,928 B) SHA-256 2d521bd21a0efa17bf24acdc97a3a8d2c279cfea1c866e90bbdce2cb89be0210; final full-health-report.json: failures=[], overallStatus=Warning, fullHealthValidated=true — profileExpectedChanges 11 AppX removals + 5 OfflineMachine/HKLM + 4 CurrentUserEffective/HKCU ALL Pass, required platform gates Pass (identity/boot-shell/devices/display/network/DHCP-IP/DNS/CheckHealth/SFC/WindowsUpdate/Security/Defender/Firewall/Store/VC++ runtime); non-blockers activation Notification + HTTPS TLS-trust Warning + optional ScanHealth NotTested. scripts/dedicated-gaming-expected-state.json built ONLY from the real selected-only plan (33/20/20 — 11 AppX removals + 5 OfflineMachine registry + 4 CurrentUserEffective registry (Start_ShowRecent=0, Start_ShowRecommended=0, EnableWebContent=0, TaskbarSearch=1); Recommend-only Containers/WSL/DevHome/OneDriveSync excluded — candidates != selected). Non-blocking environment observations recorded (one unreproducible early Setup failure with no retained log followed by a clean install; VMware guest occasional black-screen after idle/sleep; one Windows Terminal 0xD000003A; PowerShell usable through console host — none evidence of WinForge image corruption). Phase 17 delivered: deterministic validation artifact archive (.tmp/validation/<runId>/ + latest.json — runs never overwrite; runId/timestamp/source ISO identity/profile/index/edition/language/architecture/commit SHA/ISO path+SHA-256/validation level/result/phase recovery metadata), machine-readable release validation manifest (boolean evidence-gated levels; Balanced + DedicatedGaming FullHealthValidated only), remaining expected-states Gaming(19)/Developer(18)/Office(10)/Lightweight(24) derived ONLY from real selected-only plan evidence + servicesDisabled section (Lightweight 5 services), six-profile delta audit (common/exclusive/type-mix + convergence detection; no two primaries converge), release safety invariants (Defender/Firewall/Update/Store/AppInstaller/boot-shell/servicing/network/display-input/recovery/no-host-HKCU/no-unknown-mount-discard; enforced in tests), profile validation runner (--validation-run <Id> [--commit] — plan → expected-state → archive → bundle → optional chained commit; discard-only), portable FullHealth bundle (script + expected-state + manifest + README with exact -ProfileId/-MediaId/-ExpectedJson/-IsoSha256 command), RC UI audit notes, failure-recovery formalization, destructive scope explicitly deferred (ADR-086). 1300 tests (Core 53, App 1247), 0 err/0 warn. Stage 16.1 delivered the explicit --commit-profile ISO build mode, post-commit re-verification, ISO structure+SHA-256 metadata, in-VM health validator, ADR-084 levels (WorkflowValidated/VmInstallValidated/FullHealthValidated) in docs/FULL-HEALTH-VALIDATION.md; commit evidence: 16/10/10/10 postCommitVerified=true ISO structureValidated=true output Documents\WinForge\WinForge-Balanced-Win11-25H2-Pro-zh-CN-x64.iso. Previous: Phase 15 — Profile Execution: **COMPLETE (2026-08-15) — ALL STAGES 15.1/15.2/15.2b/15.3/15.3b/15.4/15.4a COMPLETE; MERGED TO `main` via `--no-ff`**; branch `phase/15-profile-execution` retained. REAL VALIDATION (Win11 25H2 Pro zh-CN x64, ISO index 4): structural — all six primaries generated non-null, conflict-free validated BuildPlans (Balanced 16, Gaming 25, DedicatedGaming 33, Developer 21, Office 17, Lightweight 38; validationPassed == true, validationErrors == []); real offline APPLY — Balanced 16 BuildPlan ops/10 selected: attempted 10, succeeded 10, failed 0, skipped 0, validationPassed true, read-back AppX + OfflineMachine registry + OfflineDefaultUser registry Verified, cleanup discard+workspace true; DedicatedGaming 33/20: attempted 20, succeeded 20, failed 0, skipped 0, validationPassed true, all 20 Verified, cleanup true; Recommend-only OptionalFeatures (Containers/WSL) were NOT executed — BuildPlan candidates != SelectedOperations proven; mounts discarded after validation (isolated discard-only workflow). Validation level per ADR-084: real-image Profile-Execution/Apply pipeline validated (WorkflowValidated) — NOT six full ISO installs, NOT VM FullHealthValidated. Phase 15 delivered: ProfileExecutionMatrix / ExecutionSupportMatrix / ProfileCandidateService / ProfileDeltaReport / ProfilePlanValidator / ProfilePlanAggregator / OptimizationDefinitionValidator / unified inventory+optimization candidate stream / canonical executable operation identity / extras semantic overrides / manual override authority / Profile → Customize → Review → BuildPlan → Apply / selected-only execution / real read-back verification / isolated discard-only validation workflow (ADR-094..097). 1225 tests (Core 53, App 1172), 0 err/0 warn. Phase 14: **COMPLETED** (real coverage validation PASSED at 89.56%, Gaming Profile 2.0, merged to `main` via `--no-ff` e308ab2; branch `phase/14-deep-component-classification` retained).
| Current Milestone | Phase 11 Stage 11.2 — Component Knowledge Import + Catalog Expansion + Customize Integration: **IMPLEMENTED** (2026-08-10), **PENDING REAL DESKTOP REVIEW**. Builds on Stage 11.1's read-only inventory (Curated 11 · DiscoveredUnclassified 734 · Protected 13 · Unsupported 0) to make curated knowledge actionable for ordinary users: a knowledge-provenance model that deliberately separates FACT from RECOMMENDATION (a community removal list never becomes WinForge "RecommendedRemove"); an offline import pipeline where candidates never auto-promote to Curated and community opinions stay informational; catalog growth 11→22 well-understood components (AV1/AVC video extensions, Bing News/Search, Calculator, Notepad, Paint, Terminal, To Do, Quick Assist, Desktop App Installer, and an "Xbox / Gaming" grouping of 9 AppX identities with a Gaming→UsuallyKeep scenario override); and the Customize **Apps tab** — the knowledge-backed decision surface (the `ComponentKnowledgeViewModel`/`ComponentKnowledgeView` engine is **repurposed as the Apps tab**, not kept as a separate tab) — presenting human 名称/作用/建议/风险 badges, a decision-oriented default sort, filters, a compact hover quick card, and **direct master–detail row selection** (click any row → open/switch the right-side detail panel; the per-row 详情 button is removed; keyboard accessible, never changes selection), conservative Protected/Unknown UX with explicit block reasons, official-vs-community evidence, and deterministic "why" captions — with no automatic destructive selection. The left-rail 组件智能/Component Intelligence page is repositioned as the advanced 高级组件检查器/Component Inspector inspection surface (raw identities shown only there / in detail / Advanced). `ComponentIntelligenceViewModel` is the shared knowledge engine; `ComponentKnowledgeViewModel` reuses its classified inventory. **556 automated tests pass (Core 53, App 503), 0 errors, 0 warnings (Release).** ADR-045/ADR-046/ADR-047/ADR-048/ADR-049/ADR-050. **Stage 11.3 — Customize Coverage Expansion + Personalization Activation — IMPLEMENTED (2026-08-11), PENDING REAL DESKTOP REVIEW:** a coverage matrix (`.tmp/phase11/stage11.3-coverage-matrix.md`) records every candidate with status/reason; the operation taxonomy (`OptimizationAction`/`OptimizationMechanism`/`OptimizationScope` as data on `CustomizationOperation`, new `DisableOptionalFeature`/`RemoveCapability` types + `FeatureConfigPolicy` allowlist) lets Review say exactly 移除/禁用/配置/服务/功能; offline registry/Default-User targeting (`DEFAULT_USER` → `<mount>\Users\Default\NTUSER.DAT`, host HKCU never touched); knowledge reuse — the Windows Components tab reuses the Stage 11.2 knowledge engine (capability/optional-feature filter), and Services/Privacy/System/Personalization share ONE catalog-driven engine; first-tranche implemented counts — Windows Components 12 · Services 12 (11 reviewed + 1 core informational) · Privacy 11 · System 10 · Personalization 14 — with the Personalization tab replacing the Experience/Coming Soon placeholder (five required groups: Start/Search, Taskbar, Explorer, Lock screen/Desktop, Appearance). **584 automated tests pass (Core 53, App 531), 0 errors, 0 warnings (Release).** ADR-051/ADR-052/ADR-053/ADR-054. Phase 10 Build/ISO Export is **COMPLETED** (real-desktop validated, merged to `main` via `--no-ff` 2026-08-10). Phase 11 remains IN PROGRESS; NOT merged to `main`. |
| Repository | https://github.com/xulu1998/WinForge |
| Platform | Windows 11 |
| Framework | .NET 8 (WPF, MVVM) |

## Progress

### Completed
- Phase 0 — Project Governance (governance docs, roadmap, agent memory system)
- Phase 1 — Application Foundation (solution, WPF shell, MVVM infra, DI, logging, error handling, Core smoke tests)
- Phase 1 headless automated validation completed (WinForge.App.Tests boot / navigation / logging integration test)
- Phase 1 real Windows desktop GUI validation completed (user-confirmed on a real Windows desktop)
- Phase 2 Step 2.1 — Read-only ISO layout inspection (ISO native file picker, file validation, read-only mount, Windows ISO candidate detection: `\boot` + `\sources` + `install.wim`/`install.esd`, WIM/ESD type, async inspection, UI busy/error state, logging, guaranteed/cancellation-safe dismount, friendly UI errors). 19 automated tests added. **Real Windows 11 25H2 (zh-CN, x64, Consumer ISO, install.wim) desktop validation completed** (mount → inspect → dismount verified by user via application logs). Accepted and merged to `main` on 2026-08-08.
- Phase 2 Step 2.2 — Windows image metadata & edition inspection (WIM/ESD index, edition name/description, architecture, Windows version, build, edition ID, installation type, languages; read-only via `dism.exe /Get-ImageInfo /ImageFile:... /English`; `IProcessRunner` abstraction; combined mount→layout→metadata→dismount session preserving ADR-015; Image page "Windows information" + editions list; edition selection → `IAppState.SelectedEdition`). **Real Windows desktop validation PASSED** (2026-08-08) on Windows 11 25H2 (Chinese Simplified, x64, Consumer Editions, `install.wim`): ISO mounted, `install.wim` detected, `/Get-ImageInfo` enumeration succeeded (6 indexes: 家庭版/家庭单语言版/教育版/专业版/专业教育版/专业工作站版), per-index detailed queries succeeded, Windows Version `10.0.26200`, Build `26200`, Architecture `x64`, Language `zh-CN`, localized Chinese edition names populated, ISO dismounted. Both real-desktop defects fixed and revalidated (DISM exit 87 via `/Get-ImageInfo`; language footer `The` rejected via `TryNormalizeLanguageTag`). 60 automated tests pass (0 errors, 0 warnings, 100% executed). **Accepted and merged to `main` on 2026-08-08.**
- Phase 2 — ISO Inspection: **COMPLETED** (closed 2026-08-08). Step 2.1 (read-only ISO layout inspection) and Step 2.2 (Windows image metadata & edition inspection) are both accepted and merged to `main`. Real Windows 11 25H2 (Chinese Simplified, x64, Consumer Editions, `install.wim`) desktop validation PASSED the Phase 2 acceptance criteria: official Microsoft ISO inspected non-destructively, image type detected, editions enumerated, architecture detected, languages detected, and `zh-CN` language confirmed (footer prose rejected). No Step 2.3 exists in the roadmap and none is invented — Phase 2 consists only of Step 2.1 and Step 2.2.
- Phase 3 Step 3.1 — WIM workspace & image selection foundation: **COMPLETED** (real-desktop validation PASSED 2026-08-09; `requireAdministrator` manifest, ADR-018). Merged to `main` via `--no-ff`.
- Phase 3 Step 3.2 — Offline WIM servicing lifecycle: **COMPLETED** (2026-08-09 on `feature/wim-servicing`; **real-desktop validation PASSED** 2026-08-09 on Windows 11 25H2 zh-CN x64 Consumer `install.wim`). Source index 4 → isolated working WIM index 1 export succeeded; isolated working-image strategy validated; real-Windows-filesystem mount verified (Windows / Program Files / Users / PerfLogs present); unmount/discard verified; `dism /English /Get-MountedWimInfo` reported no mounted images afterward; remount lifecycle passed; an active mount cannot be silently orphaned (ISO re-inspection / edition re-selection refused while Mounted); original ISO / `install.wim` / `install.esd` never modified. 133/133 automated tests pass (Core 21, App 112), 0 errors, 0 warnings. Merged to `main` via `--no-ff`.
- Phase 3 Step 3.3 — Offline customization plan & execution engine: **COMPLETED** (2026-08-09 on `feature/offline-customization`; **real-desktop validation PASSED** 2026-08-09 — re-run after ADR-026/ADR-027/ADR-028/ADR-029/ADR-030/ADR-031 defect fixes). Service-inventory safety boundary (ADR-030): discovered services are classified by `Type` + a trusted allowlist; only DiagTrack/WerSvc/PcaSvc are configurable, drivers and all other records are `Protected` and hidden/non-selectable by default in the UI. Declarative, platform-agnostic `CustomizationPlan` (Draft→Validated→Executing→Completed/CompletedWithErrors/Failed/Cancelled) plus `CustomizationOperation`/`CustomizationResult`/`DiscoveryInventory` and typed enums. Discovery: exact-identity provisioned-Appx (`DismAppxParser`, matches the real DISM single-word `PackageName`/`DisplayName` headers — removal identity is the exact `PackageName`, `DisplayName` is display-only; fixtures now contain REAL DISM output, ADR-029), allowlist-gated package enumeration (`DismPackageParser` driven by `PackageRemovalPolicy`), offline registry via Win32 `RegLoadKey`/`RegUnLoadKey` under a WinForge-owned `HKLM\WinForge_<BASE>` hive (always unloaded in `finally`; now enables `SeRestorePrivilege`/`SeBackupPrivilege` so hive load succeeds on a real elevated session), service enumeration from the mounted image `SYSTEM` hive, and a trusted `CustomizationDefinitionProvider` (5 Privacy + 3 System registry settings, 3 recommended service-start changes). Execution orchestrator with a pre-run critical-stop guard (Mounted + session match + DISM-registered + Validated → else `CriticalFailure`), frozen-snapshot ordered execution (registry→services→appx→packages→files→tasks), package-removal allowlist (non-allowlisted packages `Skipped`, never removed), and it **leaves the image mounted** (no commit/unmount). UI: Components/Privacy/System/Plan pages backed by discovery, selection→plan operations, explicit Validate+Apply; navigation + DI wired. Safety: `DiscoveryInventory` now surfaces per-source `Appx/Package/Service` success/failure status (a DISM/registry failure is reported, never silently zero — genuine zero is a legitimate `Success(0)`, distinct from failure/unrecognized, ADR-029); the single `PackageRemovalPolicy` governs discovery, plan validation, and execution so no non-allowlisted package can ever be selected or removed (ADR-026/ADR-027). 224 automated tests pass (Core 37, App 187), 0 errors, 0 warnings; CI-safe (no ISO/admin/internet). Not merged to `main`; no new tag.

- Phase 3.5 — UX Workflow Refactor + Localization Foundation: **COMPLETED** (real-desktop validation PASSED 2026-08-09 on Windows 11 25H2 zh-CN x64 Consumer `install.wim`; merged to `main` via `--no-ff`). Wizard/Stepper primary workflow over the Step 3.3 engine (no DISM in workflow code). **Real-desktop acceptance chain (all PASS):** (1) Source ISO selection + ISO inspection; (2) Prepare isolated working image; (3) Mount working WIM; (4) Mounted → Next enables; (5) Customize page loads; (6) all Customize tabs load with no XAML/binding crash; (7) runtime localization / zh-CN UI works; (8) discovery + selections work; (9) selecting 1 valid customization enables Review/Next; (10) Review page loads; (11) plan validation succeeds; (12) Apply to mounted image succeeds; (13) execution success advances Wizard state (Review→Completed, Apply→Available, Next→enabled); (14) Build placeholder reachable; (15) Build/ISO export intentionally NOT implemented (honest placeholder, not a validation failure); (16) working image cleanly unmounted/discarded, no mounted WIM remains. **362 automated tests pass (Core 37, App 325), 0 errors, 0 warnings (Release).** ADR-032…ADR-037. Feature-branch real-desktop defect fixes: ISO mount robustness / valid Get-Volume resolution (`7f90d48`/`4360612`), servicing workspace nested notification (`28b8bb5`), `CustomizationPlan` nested notification (`06de8f9`), `Run.Text` MultiBinding `Mode=OneWay` (`f1e4370`), read-only binding `OneWay` audit (`9f68f97`), `ErrorDialogGuard` (`bed2e73`), Review/Apply gating on execution success (`7da38dd`).

- Phase 10 — Build / ISO Export: **COMPLETED** (real-desktop validation PASSED 2026-08-10 on Windows 11 25H2 Chinese Simplified x64 Consumer `install.wim`; merged to `main` via `--no-ff` on 2026-08-10). Replaces the honest placeholder Build step with a real, safe ISO-rebuild pipeline. `ImageBuildService` orchestrates a 6-phase state machine — `Preflight → CommittingImage → ExportingImage → PreparingMedia → BuildingIso → Verifying → Completed/Failed/Cancelled` — delegating to: `IImageServicingService.CommitUnmountAsync` (DISM `/Unmount-Image /Commit`; `/Discard` never used on the build path, ADR-039); `IWimExporter`/`DismWimExporter` (`DISM /Export-Image /Compress:max /CheckIntegrity`, clean install.wim, ESD normalized to a WIM at index 1, ADR-040); `IIsoMediaPreparer`/`IsoMediaPreparer` (read-only copy of the original media tree, replaces `sources\install.wim` or deletes `install.esd` and writes `install.wim`, validates dual-boot files, ADR-040); `IBootableIsoBuilder`/`OscdimgIsoBuilder`/`OscdimgArgumentBuilder` (Windows ADK `oscdimg.exe`, dual-boot args, fails fast and clearly when ADK missing or a boot file absent — never fakes an ISO, ADR-041); `IBuildVerifier`/`BuildVerifier` (independent re-check: output exists + size, install.wim present, no mounted WIM, expected edition/index; a failed verification fails the build, ADR-043). `BuildStepViewModel` derives inputs from `IAppState`, gates `CanBuild` (Applied + Mounted + ADK + non-empty paths), defaults the file name to `WinForge_<Edition>_<yyyyMMdd-HHmm>.iso` (spaces→`_`), uses an explicit overwrite policy (default `GenerateUniqueName`), runs a cancellable `AsyncRelayCommand`, and surfaces the terminal `BuildState` + output path/size + log from `BuildResult` (ADR-042). `ImageBuildService` writes an atomic `build.recovery.json` for crash recovery (ADR-043), and a **resumable post-commit build checkpoint** lets a failed post-commit phase resume without re-Apply. The ISO media copy clears only ReadOnly/System/Hidden attributes on the **build copy** (never the source) and cleans the media tree deterministically; the final Build step shows a completion-gated localized **Finish** that navigates Workflow → Home without deleting the ISO (ADR-044). **440 automated tests pass (Core 37, App 403), 0 errors, 0 warnings (Release).** ADR-038…ADR-044. All Phase 10 tests are CI-safe (no ISO / admin / internet) via fakes; the source ISO is never modified.

### In Progress
- **Phase 11 — Component Intelligence Foundation (COMPLETED, REAL DESKTOP VALIDATED, MERGED TO `main` via `--no-ff` 2026-08-12).** Stage 11.1 — Full Offline Component Inventory + User Decision Model — is **REAL DESKTOP VALIDATED** (2026-08-10) on branch `phase/11-component-intelligence` (NOT merged to `main`). Stage 11.2 — Component Knowledge Import + Catalog Expansion + Customize Integration — is **IMPLEMENTED** (2026-08-10), **PENDING REAL DESKTOP REVIEW**. Stage 11.2 delivers: a knowledge-provenance model (`KnowledgeSource`/`KnowledgeClaim`/`ScenarioRecommendation` — `KnowledgeClaimKind.Fact` vs `Recommendation` deliberately separated so a community removal script never becomes WinForge "RecommendedRemove"); an offline import pipeline (`KnowledgeImportPipeline` + `IKnowledgeSourceAdapter`s MicrosoftOfficial/WindowsImageDiscovery/Win11DebloatCommunity/WinForgeCurated — candidates never auto-promote to Curated, community `CommunityProposal` never promotes to `EffectiveRecommendation`); catalog expansion 11→22 well-understood components (`CuratedComponentCatalog`, generated by `.tmp/phase11/gen_catalog.py` — AV1/AVC video extensions, Bing News/Search, Calculator, Notepad, Paint, Terminal, To Do, Quick Assist, Desktop App Installer, and an "Xbox / Gaming" grouping of 9 AppX identities with a Gaming→UsuallyKeep scenario override; each carries provenance claims); and the Customize **Apps tab** — the knowledge-backed decision surface (the `ComponentKnowledgeViewModel`/`ComponentKnowledgeView` engine is **repurposed as the Apps tab**, not kept as a separate tab) — reusing `ComponentIntelligenceViewModel`'s classified inventory: human 名称/作用/建议/风险 badges, decision-oriented default sort, filters, compact hover quick card, explicit ⓘ 详情 detail action (keyboard/touch accessible, no selection change), conservative Protected/Unknown UX with explicit block reasons, official-vs-community evidence, deterministic "why" captions, no automatic destructive selection. The left-rail 组件智能/Component Intelligence page is repositioned as the advanced 高级组件检查器/Component Inspector inspection surface (raw identities shown only there / in detail / Advanced). **534 automated tests pass (Core 53, App 481), 0 errors, 0 warnings (Release).** ADR-045/ADR-046/ADR-047/ADR-048. **Real Windows 11 25H2 zh-CN x64 Consumer ground-truth enumeration PASSED:** Component Intelligence page renders correctly; real mounted-image discovery succeeds; Standard mode shows only the 11 curated components; Advanced mode exposes real discovered raw Windows objects for inspection; raw unclassified objects are NOT presented as trusted/removable items; curated detail panel renders recommendation/risk/impact/restore/technical metadata. Real classification counts: **Curated 11 · DiscoveredUnclassified 734 · Protected 13 · Unsupported 0**. PRODUCT CONCLUSION: the 734 discovered raw objects must NOT become 734 normal removal checkboxes — they stay raw Windows identities and are surfaced only in Advanced mode; Stage 11.2 turns representative families into user-understandable logical components (Unknown stays Unknown until evidence-backed). Stage 11.2 (next, **NOT STARTED**): Component Catalog Expansion — classify representative real unclassified objects into logical human component groups with purpose/risk/keep-if/remove-if/impact/restore, starting with high-value low-risk families (video codecs/extensions, news/search consumer apps, gaming/Xbox, Desktop App Installer, Quick Assist, Paint, Notepad, Calculator, Terminal, To Do, media AppX, optional consumer apps); no deep CBS removal; Protected items never exposed for removal; no dependencies inferred without evidence.

### Next
- _(none — Phase 10 Build / ISO Export is **COMPLETED** (real-desktop validation PASSED, merged to `main` via `--no-ff` on 2026-08-10). No Step 3.4 / Phase 4 mount engine in this scope.)_

### Maintenance
- 2026-08-12 — **Phase 11 Stage 11.4 — Scenario Profile / Recommended Configuration Engine — IMPLEMENTED (`phase/11-component-intelligence`, PENDING REAL DESKTOP REVIEW; NOT merged to `main`).** Per ADR-057..060: (1) `ProfileDefinition` (Core) models priorities — RecommendationOverrides (logical-id Keep/Trim + reason key), RequiredCapabilities (present-gated hard keep), PreferredCapabilities, AvoidedComponents — never raw package names; 7 reviewed profiles generated by `gen_stage113.py` → `ProfileCatalog` (Balanced/Gaming/Developer/Office/Lightweight/DedicatedMinimal/Custom; multi-select supported; Custom is exclusive). (2) `RecommendationEngine` (Core, pure) computes `EffectiveRecommendation` separately from the definition default with documented precedence (safety > user override > required dependency > profile requirement > scenario override > component default); conflicts resolve visibly — KEEP wins when a selected scenario requires the component — with `RecommendationConflict` + deterministic localized reason keys (Part F, no runtime AI prose). (3) UX: profile selector cards at the top of Customize (Part H); non-destructive 查看推荐方案 preview grouped 推荐执行/建议保留/需要确认/冲突·阻止 (Part I); 采用推荐选择 is the ONLY selection-changing action, limited to present + apply-supported + low-risk + conflict-free + non-overridden items (Part J); 重新应用推荐 re-runs the same eligibility; manual toggles mark user overrides that survive recalculation (Part K); category-aware captions (Apps 推荐移除 / Feature 推荐禁用 / Privacy 建议关闭 / Service 建议保持默认) via effective levels (Part L); per-workflow state resets on a new image, fresh workflow defaults to no profile (Part Q). **Real-desktop UX fix (2026-08-12):** compact profile panel (name + short subtitle cards, ≤200px), primary action = 采用推荐选择 with 查看推荐详情 demoted to a secondary link, conditional 重新采用推荐 visible only after adoption + divergence, Custom = no overrides (returns to catalog defaults, preserves manual selections), Discover button flips to 重新扫描 after discovery, and the Customize tabs/list keep the majority of the viewport (STA height test pins panel ≤260px and TabControl ≥250px at 1200×700). **Profile UX + recommendation-quality rework (2026-08-12, real-desktop review):** ONE primary profile (mutually-exclusive radio cards in a 3-column equal-width Grid — 均衡推荐/游戏优先/开发工作站/办公稳定/轻量系统/专用精简/自定义) + optional EXTRA scenarios (Xbox/Game Pass, WSL/Docker, 打印扫描, 触屏/手写, 远程桌面 — independent checkboxes, engine still combines internally); each primary profile expanded to a meaningful multi-tab configuration (impact report `.tmp/phase11/stage11.4-profile-impact.md` — modeled-fixture automatic actions: Balanced 19 · Gaming 21 · Developer 24 · Office 15 · Lightweight 30 · DedicatedMinimal 37, none flagged ≤3); preview detail groups decisions by bucket with per-tab breakdown (Part 11); medium-risk trims land in 需要确认 and unsupported items in 冲突/不可执行 (Part 12); every profile-driven row attributes the advising profile (配置建议: 游戏优先 → 建议保留, Part 13). 57 `Stage11p4Tests` + ProfileView STA layout test. **Final compact Customize layout (2026-08-12, approved direction):** CustomizeView rebuilt as a 5-row Grid — compact header (title 20px + subtitle + [重新扫描] 已选 N 项 share ONE row; measured 77px, ~45% smaller), the ProfileView two-panel area, and a STAR-height TabControl that consumes all remaining space (measured 372px of 700px viewport → ~7 visible rows at 1200×700; maximized ~10+). ProfileView is a real two-column Grid — primary profile radio cards LEFT (3 equal columns, ~46px cards, measured 883px) vs extra scenarios RIGHT (5 checkboxes, measured 294px, 75:25); summary + actions (采用推荐选择 / 查看推荐详情 / conditional 重新采用推荐) stay on ONE line; no horizontal scrollbars; recommendation engine untouched (Part L). **Final-flow rework (2026-08-12, real-desktop feedback):** selecting a Profile IS the adoption — the separate "采用推荐选择" button and AdoptCommand are REMOVED; picking Gaming/Developer/Lightweight immediately applies its safe recommended selections (Part J eligibility unchanged: present/apply-supported/Low risk/trusted/no conflict/not protected/not AdvancedOnly/not NeverRemove). Selection provenance: `RecommendationContextService` tracks Profile-managed ids (`IsProfileManaged`) vs user overrides (`HasUserOverrides`/`ClearUserOverrides`); switching profiles re-applies Profile-managed rows (adds new recommendations, drops no-longer-recommended Profile-managed rows) while user overrides ALWAYS win; Custom preserves the current plan (no clear) and returns to manual mode; conditional "恢复此配置推荐" (`RestoreCommand`/`RestoreVisible`) appears only when overrides exist and is the only explicit path that overwrites them. 查看推荐详情 moved to a Customize OVERLAY (dimmed backdrop + panel, × 返回自定义) so the tabs/list stay intact and the user is never trapped. Header "已选 N 项" now counts the shared plan (`CustomizeStepViewModel.SelectedTotal` via `PlanSync.PlanChanged`) so profile auto-apply updates it immediately. Rows expose `SelectionOriginText` (由「X」自动选择 / 手动选择). 11 new final-flow tests (T1-T17 incl. no-Adopt-button, immediate count, medium/unsupported stay manual, override survives switch, Custom preserves plan, Profile-managed re-apply, detail overlay open/close/state-restore, no auto-navigation, zh/en origin text). (model round-trip, multi-scenario, precedence, dependency/override/safety, Gaming/Developer/Office/Lightweight rules, conflict resolution, preview/adopt/override UX, en/zh localization, profile change never mutates the plan); ProfileView added to the WPF binding audit. Full suite **630 pass (Core 53, App 577), 0 errors, 0 warnings (Release)** via non-colliding OutDir (the user's running app instance locks `bin/`). Docs updated (PROJECT_STATUS/ROADMAP/ARCHITECTURE/DECISIONS ADR-057..060/CHANGELOG). Stage 11.3 is **REAL DESKTOP VALIDATED** (2026-08-12). STAGE 11.4 — PENDING REAL DESKTOP REVIEW; Phase 11 remains IN PROGRESS; NOT merged to `main`.
- 2026-08-12 — **Phase 12 — Workspace Lifecycle & Disk Safety — IMPLEMENTED (`phase/12-workspace-lifecycle`, PENDING REAL DESKTOP VALIDATION; NOT merged to `main`).** Root cause of the 249 GB leak (≈30 stale wf-* × ~6.81 GB + temp output): workspaces had NO durable lifecycle and nothing ever cleaned them. ADR-062..065: (1) every workspace persists `workspace.json` (explicit lifecycle state Created→…→Completed/FailedDisposable/Cancelled/Cleaned; transition log); the servicing service transitions it on Prepare/Mount/UnmountDiscard/UnmountCommitted/PrepareFailed and the build view marks Completed + FinalOutputPath. (2) Cleanup safety: the LIVE DISM `/Get-MountedImageInfo` registration is authoritative — a registered mount (or any mount nested in the workspace dir) is never deleted, NeedsRemount is surfaced for recovery, mount-query failure fails closed, recoverable checkpoints are retained, and Completed-with-output is disposable. Cleanup strips ReadOnly/System/Hidden, reports reclaimed bytes, records exact leftover paths on partial failure, retries later. (3) Output/temp separation: final ISO default moved to `Documents\WinForge` (never disposable temp). (4) Disk guard: `DiskSpaceEstimator` (Prepare ≈ WIM×4+2GiB; Build ≈ WIM+media+ISO+2GiB) blocks before build with a localized insufficient-space message. (5) Settings now hosts a **Storage** surface: async scan of wf-* workspaces (temp/recoverable/active/disposable totals), 清理临时文件 preview (可安全清理 / 将保留), legacy leftovers flagged 旧版残留, one-click cleanup; BuildStepViewModel marks completed workspaces cleanup-eligible. **701 automated tests pass (Core 53, App 648), 0 errors, 0 warnings (Release)** — incl. 23 new `Stage12p1Tests` (manifest/transitions, discard→eligible, completed→eligible, ISO never deleted, mounted-never-deleted, needs-remount, fail-closed, checkpoint retained, failed-disposable cleaned, legacy detected/protected, corrupt manifest safe, size/bytes, ReadOnly/System/Hidden cleanup, partial-failure reporting, root-scoped workflow isolation, repeated-workflow non-accumulation regression for the incident). Docs updated (PROJECT_STATUS/ROADMAP/ARCHITECTURE/CHANGELOG/DECISIONS ADR-062..065). **Stage 12.2 — Finish cleanup + workspace-root settings — IMPLEMENTED (2026-08-12).** (1) Workspace-root UI in Settings → 存储: current root + free space + [更改位置]/[恢复默认位置]; path validation (drive/profile root rejected, creatable+writable probe), active-mounted session blocks the change, low-space warning, persists across restart via `%LOCALAPPDATA%\WinForge\workspace-roots.json` (`IWorkspaceRootSettingsService`). (2) Multi-root discovery (Part G): every known root (current + previous) is scanned for cleanup/orphans; changing root never moves existing workspaces. (3) Finish auto-cleanup (Part C/D): `WorkflowViewModel.FinishAsync` runs the authoritative DISM-safe cleanup of the completed workspace — final ISO preserved, recoverable checkpoints retained, bytes reclaimed reported on the Build step (ISO 已生成 — 已清理临时文件：X GB / 部分未能清理 [立即重试清理] / 保留恢复数据：X GB); a cleanup failure is a WARNING, never a build failure. (4) Discard auto-cleanup (Part E): a successful Unmount/Discard cleans the disposable workspace in the background. 16 new `Stage12p2Tests` (root persistence/restore/new-workflow-root/old-root-kept/mounted-blocks-root/invalid-root-rejected/multi-root-scan/finish-cleans-completed/ISO-survives/mounted-blocks-finish-cleanup/recoverable-retained/bytes-reported/discard-auto-clean/failure-not-build-fail/retryable/zh-en). **701 automated tests pass (Core 53, App 648), 0 errors, 0 warnings (Release)**. PHASE 12 — IN PROGRESS (Stage 12.1 + 12.2 IMPLEMENTED); PENDING REAL DESKTOP VALIDATION; NOT merged to `main`. **Stage 12.2 real-desktop blocker FIXED (2026-08-12):** Settings → 存储 threw 'System.Windows.Documents.Run.Text' exceptions (multiple global error dialogs) because the usage line was a `<Run>` inline composition that only materialized when the scanned-state panel became visible during layout — compile checks passed and the render smoke test measured the still-Collapsed panel, so neither caught it. Replaced with a single stable TextBlock.Text binding to the fully formatted, localized `StorageViewModel.UsageSummaryText`; StorageView.xaml now contains zero Run/Inline. Also fixed `CleanAsync` wiping its own 已清理 X result via the follow-up re-scan (`ScanAsync(clearResultText:)`). New `StorageViewRenderRegressionTests` (9 tests, zh-CN + en-US) build the real VM against the real resx service, activate the scanned state, force a full layout pass and assert every display state renders without exception, plus a guard asserting no `<Run>` can return; the binding audit Storage cases now render the scanned and root-error states. **710 automated tests pass (Core 53, App 657), 0 errors, 0 warnings (Release)**. **Stage 12.3 real-desktop blocker FIXED (2026-08-12):** Review 「校验计划」appeared to do nothing and 「应用到已挂载镜像」stayed disabled. Root cause: (1) successful validation gave no visible feedback (no success message); (2) a FAILED validation (blocking duplicate/conflict issues) kept showing 「没有校验警告」 because Warnings was replaced without notifying the derived HasWarnings — the real blocking reason was invisible while Apply stayed disabled; (3) a throwing validator was silent. Fix: ValidatePlan sets explicit localized outcome (ValidationPassed / ValidationMessage / HasValidationFailure, green 计划校验通过，可以应用修改。 / red 计划校验失败：N 个阻塞问题…), Warnings notifies HasWarnings, exceptions surface + log (never silent), Apply/Next re-evaluate immediately; new resx Review.ValidatePassed/ValidateFailed/ValidateError. 14 new Stage12p3ReviewValidationTests + PlanReviewView Validated/Failed binding-audit render cases. **724 automated tests pass (Core 53, App 671), 0 errors, 0 warnings (Release)**. **Stage 12.4 real-desktop blocker FIXED (2026-08-12):** after the Stage 12.3 validation-UX fix the validator correctly surfaced a duplicate — but the PLAN COMPILER must not emit two identical physical operations in the first place. Root cause: Privacy `SpotlightFeatures` ("Windows 聚焦内容") and Personalization `DisableSpotlight` ("Windows 聚焦（锁屏内容）") both compile to the SAME `SetOfflineRegistryValue` (CloudContent\DisableWindowsSpotlightFeatures = DWord 1, OfflineMachine), differing only in OperationId. Fix (ADR-068): canonical effective-target identity (scope + normalized hive/key/value-name) + mutation-semantics comparison on `CustomizationOperation`; `CustomizationPlan.AddOperation` merges identical effective registry changes into ONE physical operation with provenance retained (`SourceDefinitionIds`), keeps semantically different mutations as blocking conflicts, and `ConflictKey` now includes scope (no cross-scope false positives); `OptimizationKnowledgeItem` records definition-id provenance. Validator NOT weakened. 15 new Stage12p4PlanNormalizationTests; full catalog (Gaming/Lightweight/Developer supersets) compiles + validates. **739 automated tests pass (Core 53, App 686), 0 errors, 0 warnings (Release)**. **Stage 12.5 real-desktop blocker FIXED (2026-08-12):** a fully successful build (7.62 GB ISO at Documents\WinForge) left the wizard stepper at 构建镜像 · 进行中 and 完成 disabled. Root cause in WorkflowViewModel: OnBuildChanged refreshed CanFinish but never called RecomputeStates (Build step stayed Current), and it raised FinishCommand with an `is RelayCommand` check while FinishCommand is an AsyncRelayCommand — so RaiseCanExecuteChanged() never fired and the button stayed disabled despite CanFinish being true. Fix (ADR-069): OnBuildChanged -> RecomputeStates; Build step branch maps current+Completed to Completed; FinishCommand refreshed via the correct AsyncRelayCommand type. Finish gating unchanged (NotStarted/Failed/Cancelled/Verifying-failed all keep it disabled); Stage 12.2 Finish cleanup preserved. 9 new Stage12p5BuildFinishStateTests (start/success/CanExecuteChanged/failure/cancel/verify-fail/survive-refresh/cleanup+ISO/Home/zh-en). **748 automated tests pass (Core 53, App 695), 0 errors, 0 warnings (Release)**. **Stage 12.6 real-desktop defect FIXED (2026-08-12):** applying 隐藏小组件按钮 threw "Attempted to perform an unauthorized operation" (UnauthorizedAccessException) while sibling TaskbarSearch (same hive, same Explorer\Advanced key) succeeded — the old target lives in the protected Explorer subtree of the Default User template (template ACLs reject offline writes). Fix (ADR-070, Case D): HideTaskbarWidgets now targets the official user-policy branch Software\Policies\Microsoft\Dsh → EnableWebContent = 0 (25H2 supported mechanism); EnsureKeyPath probes read-only keys and raises an explicit "read-only template ACL" failure (no ACL/ownership rewriting, ever). Apply UX: localized 应用完成：{0} 项成功，{1} 项失败。+ visible failed-operations panel; per-operation outcomes now write back from the execution snapshot onto the LIVE plan (previously frozen-snapshot execution left live ops Pending so failures were invisible). Partial apply never silently treated as success. 10 new Stage12p6HideWidgetsTests + PlanReviewView.ApplyFailed binding-audit case. **758 automated tests pass (Core 53, App 705), 0 errors, 0 warnings (Release)**. **Stage 12.7 real-desktop blocker FIXED (2026-08-12):** CurrentRoot=F://WinForgeWorkspaces was bypassed — a ~6.9 GB workspace (wf-a9bac38c7259) kept reappearing under the old C: default root after Finish. Root cause (SPLIT workspace): WorkspacePathProvider was registered with a standalone default root and never read WorkspaceRootSettingsService.CurrentRoot — Prepare created servicing data under C: while the lifecycle manifest went to F: (two same-id directories); Finish cleaned the F: manifest shell and leaked the C: data. Fix (ADR-071): WorkspacePathProvider resolves CurrentRoot at runtime; Bootstrapper wires it to the settings service, so every new workflow path (Prepare/Apply/Commit/Export/checkpoint/Build) creates only under CurrentRoot; KnownRoots stay scan/recover/clean-only (never a creation destination); Finish cleans the unified workspace; ISO (outside workspace) survives; Storage candidates display their owning root. 9 new Stage12p7WorkspaceRootTests incl. the full incident reproduction (KnownRoots=[C,F], CurrentRoot=F → F-only creation, F cleaned, C untouched). **767 automated tests pass (Core 53, App 714), 0 errors, 0 warnings (Release)**. **PHASE 13 — COMPATIBILITY FOUNDATION IMPLEMENTED (2026-08-12; branch `phase/13-compatibility-matrix`, NOT merged).** Compatibility model (CompatibilityStatus/Severity/ImageCompatibilityProfile/Findings), release classifier (24H2/25H2/unknown-newer/older; unknown future degrades to SupportedWithWarnings), edition capability facts (Sandbox/Hyper-V/RDP host per edition), language stable-identity policy, WIM/ESD/SWM detection + multi-index enumeration/index persistence + conservative media classification (MicrosoftOfficialLike/ModifiedMedia — never overclaims "official"), split-WIM read-only marking. Compatibility preflight surfaced after ISO inspection (concise release · status + inline blockers/warnings); blocking vs warning rules (unsupported arch / missing install image / missing boot.wim block; future build / modified media / untested edition warn only). Validation matrix data model + initial targets (Tier A 25H2 Pro zh-CN+en-US, Tier B Home/Education/Enterprise, Tier C 24H2/ESD/multi-index) + JSON/Markdown report export (validation/) with strict Validated-vs-Automated evidence separation (ADR-074). Safety invariants asserted against real catalogs (Update services, Defender, core drivers, Store). docs/COMPATIBILITY.md + ADR-073..078. 31 new Stage13CompatibilityTests. **798 automated tests pass (Core 53, App 745), 0 errors, 0 warnings (Release)**. **Stage 13 preflight UI real-desktop blocker FIXED (2026-08-12):** after 检测 on the real 25H2 zh-CN ISO (10.0.26200, Pro index 4) the compatibility card was absent — the profile setter's SetField only notified "CompatibilityProfile" while the View binds to DERIVED properties (StatusText/DetailsText/HasCompatibility*) that were never notified; the earlier notify sat in the SelectedEdition setter (runs BEFORE the profile is assigned). Fix (ADR-079): notify derived properties from the profile setter + SelectedEdition setter; section gated by HasCompatibilityProfile with always-visible details; edition/index/language reflect the SELECTED edition and refresh on selection change (Home → Core facts, Pro → Professional + Index 4) without re-detect; wording stays "支持" (workflow-validated baseline, VM validation pending — never "完整验证通过"). 10 new Stage13PreflightUiTests incl. REAL WPF render assertions. **808 automated tests pass (Core 53, App 755), 0 errors, 0 warnings (Release)**. **Stage 13 compatibility-UI diagnostic (2026-08-12):** after TWO fixes the real desktop still showed no compatibility status. Deterministic instrumentation found the root cause (category A): the wizard renders the Source step via `SourceView` (App.xaml SourceStepTemplate), while `ImageView.xaml` is a LEGACY view never referenced by any template — both earlier compatibility UI rounds were added to the wrong view. Diagnostic commit adds a literal `PHASE13-COMPAT-DIAG · <sha>` marker + always-bound `CompatibilityDebugText` + unconditional compatibility row to BOTH SourceView and ImageView, plus PHASE13 COMPAT runtime logging; compiled BAML verified to contain the marker. 6 new Stage13DiagnosticMarkerTests (marker in both views, DebugText never empty, 26200 → Profile=True dump, no-Visibility row, real SourceView render shows marker+debug+row). **814 automated tests pass (Core 53, App 761), 0 errors, 0 warnings (Release)**; diagnostic marker removed after real-desktop confirmation. **Stage 13 preflight final cleanup (2026-08-12):** diagnostic root cause CONFIRMED (category A — SourceView is the runtime view); all PHASE13 diagnostics removed; compatibility row lives in SourceView only (ImageView has none). Real-desktop display fixes: `Compat.Edition.` leak eliminated (L() never returns raw keys for missing entries; LocalizeEditionName empty-id guard); `Lang=?` fixed (SelectedEdition.DefaultLanguage → profile.DefaultLanguage → AvailableLanguages[0]); `Index=none` fixed (row shows Index <n> from the current SelectedEdition only, refreshes instantly). Final row: 兼容性 `Windows 11 25H2 · 专业版 · x64 · zh-CN · WIM · Index 4 · ✓ 支持`. STA render tests serialized via shared WpfRenderLock.Sync (fixed intermittent parallel STA races). **823 automated tests pass (Core 53, App 770), 0 errors, 0 warnings (Release)** (OutDir; in-place build pending — WinForge.App.exe was running). **Phase 13 VM validation preparation (2026-08-13):** sandbox cannot run a VM — nothing fabricated. Latest generated ISO located (8,177,487,872 B, SHA-256 21311def…cae98, 25H2 build 26200 zh-CN x64 WIM, Pro idx4); runbook docs/VM-VALIDATION.md (Hyper-V Gen-2 UEFI + 25-item checklist) and report template validation/25H2-Pro-zh-CN-x64-20260813-0720.{json,md} (Evidence=NotRecorded) prepared; docs/COMPATIBILITY.md row stays pending until RealVmValidation passes. Phase 14 input recorded (docs/PHASE14-INPUT.md: Gaming profile direction from real user feedback). **STATUS: PENDING USER REAL VM VALIDATION** — the Phase 13 close gate. **PHASE 13 — COMPLETED (2026-08-13):** baseline VM validation ACCEPTED (ADR-084) — generated ISO boots Hyper-V Gen-2 UEFI, Setup/install/reboot/OOBE/desktop PASS → Level = **VmInstallValidated** (ValidationLevel semantics Workflow/VmInstall/FullHealth added; AllPhasesPassed evaluates the Level-required phase set). Deeper health checks intentionally deferred to later aggressive component-removal phases (documented). docs/COMPATIBILITY.md: 25H2 Pro zh-CN → **VM INSTALL VALIDATED** (not FullHealthValidated); en-US/Home/Edu/Enterprise/24H2/ESD/SWM pending; ARM64 unsupported. Validation record updated (RealVmValidation, VmInstallValidated, no fabricated checks). Phase 14 input (Gaming profile) recorded. **824 automated tests pass (Core 53, App 771), 0 errors, 0 warnings (Release)**. **MERGED TO `main` via `--no-ff` (2026-08-13).** **PHASE 14 — STAGE 14.1 COMPLETE (2026-08-13):** deep component taxonomy + classification foundation (ADR-085): discovery/knowledge/planning separation; ComponentFunctionCategory (25 categories); DeepComponentKnowledge model (risk/recommendation/protection/profile/confidence); ComponentNormalizer (token stripping + collision guard); first-batch DeepComponentCatalogData = **108 curated family entries** across Gaming/consumer/search/telemetry/printing/virtualization/dev/remote/media/cloud/Store/runtime/security/servicing/recovery; protected groups (servicing/update/CBS/Defender/boot/WinRE/App Installer/Store/WebView2/runtimes/DirectX/shell); Gaming profile foundation metadata (Gaming PC vs Dedicated Gaming; no placebo tweaks); UI: classified DiscoveredUnclassified rows now surface in the existing Apps/Windows Components knowledge tabs with name/purpose/recommendation/risk; coverage metrics keep Unknown visible as debt. **845 automated tests pass (Core 53, App 792), 0 errors, 0 warnings (Release)**. **STAGE 14.2 — REAL-MEDIA FAMILY EXPANSION COMPLETE (2026-08-13):** real ISO confirmed (25H2 zh-CN x64 Consumer, install.wim extracted read-only); sandbox non-elevated → DISM Error 740 blocks fresh scan, real baseline = Phase 11 elevated scan (758: 11 curated + 734 unclassified + 13 protected). Deep catalog 108 → **145 entries** (+22 CBS family rules, +15 hardware rules; CBS floors Risk≥Moderate/Protection≥Sensitive/never RecommendedRemove). UnknownFamilyAnalyzer (ranked family debt) + enhanced metrics (Curated/KnownDeep/Protected/Heuristic/Unknown per source, no double count) + restrained CoverageSummaryText UI. docs/COMPONENT-COVERAGE.md (honest coverage estimate ≥60% family-level; exact numbers need elevated scan; classified ≠ removable). **892 automated tests pass (Core 53, App 839), 0 errors, 0 warnings (Release)**. **STAGE 14.3 — IMPLEMENTATION READY (2026-08-13):** (A) ELEVATED REAL INVENTORY CAPTURE — new `tools/WinForge.RealCapture` console CLI runs the EXACT production pipeline elevated (inspect ISO → export selected index → mount → production DISM discovery → matcher → DeepComponentClassifier → exact no-double-count coverage accounting → UnknownFamilyAnalyzer top-30 → JSON exports to `.tmp/phase14-real/` → unmount+discard+cleanup; source ISO read-only; `requireAdministrator` manifest; Error-740 guidance). Core `CoverageAccountingService` (exact per-source buckets Curated/KnownDeep/Heuristic/Unknown; Protected property count; reconcile invariant) + `MatcherProtected`/`TotalClassifiedRatio` metrics. **Exact real numbers require the user to run the capture as Administrator — none fabricated.** Real-derived stable fixture `tests/fixtures/25H2-Pro-zhCN-component-families.json` (+validator tests; refreshable by the CLI output). (B) UNKNOWN-FAMILY mechanism ready (top-30 real report produced by the CLI). (C) GAMING PROFILE 2.0 (ADR-088/089/090): knowledge-driven pipeline Inventory→Deep Knowledge→Profile Policy→Candidate→Safety Gate→Plan; `GamingPcPolicy`/`DedicatedGamingPolicy` (two DISTINCT primaries, never aliases); `ProfileSafetyGate` final authority (Protected/Critical/High block, Moderate→optional, Low+curated→auto, heuristic never auto, unsupported blocks, user override preserved); extras materially influence (Xbox/Game Pass, WSL/Docker, print/scan, touch/pen, Remote Desktop); §8 keep list (servicing/update/Defender/Store/winget/Gaming Services/runtimes/DirectX/audio/networking/GPU/USB/storage/shell/WinRE/boot); NO placebo tweaks (HPET/BCD/tick/memory/pagefile/cargo-cult forbidden); deterministic localized reasons (en+zh-CN resx); localized Gaming profile summary (推荐修改/为兼容保留/可选建议 + bounded examples, no hundreds of ids); manual overrides stay authoritative; `Gaming` primary = Gaming PC, new `DedicatedGaming` primary added. **975 automated tests pass (Core 53, App 922), 0 errors, 0 warnings (Release)** — in-place ordinary `dotnet build`/`dotnet test` on the real machine pass after the external obj-cache lock released (reported exact path earlier: `src/WinForge.Core/obj/Release/net8.0/*.cache` Permission denied while locked). Docs: ADR-087..090, COMPONENT-COVERAGE/TAXONOMY/PHASE14-INPUT, PROJECT_STATUS/ROADMAP/CHANGELOG/ARCHITECTURE. **STAGE 14.3b — IMPLEMENTATION READY (2026-08-14, ADR-091):** FIRST ELEVATED RealCapture SUCCEEDED on the real desktop (Administrator) — EXACT real numbers: Total 757 (AppX 47: curated 22/protected 3/known 10/unknown 15 · Capability 425: 2/3/8/415 · CbsPackage 149: 0/27/106/43 · OptionalFeature 136: 8/4/77/51); Overall Curated 32 · Protected 37 · KnownDeep 201 · Heuristic 0 · **Unknown 524 · coverage 30.78%** (new explicit accounting boundary: only AppX/Capability/OptionalFeature/CbsPackage; Service/Driver/Language/etc. = NotSupported — NOT mechanically comparable to Phase 11's 758). Six real Language capability families classified (Basic 123/Handwriting 89/TextToSpeech 49/OCR 35/Fonts 24/Speech 17 = 337 objects; Function=Language, Moderate, ProfileDependent, Sensitive; one family per role, locale identity preserved via `LanguageCapabilityMetadata`; target-language metadata prepared, NO destructive stripping). Family analyzer refined: `Microsoft.Windows.*` dotted capabilities keep up to 5 semantic segments (Console.Legacy / Ethernet.Client.Intel / Ethernet.Client.Realtek / Wifi.Client.* distinct; trailing generic role words dropped); `Package_for_<sem>_<num>` CBS semantics preserved by the normalizer (DotNetRollup→dotnetrollup, KBxxxx→kb, RollupFix→rollupfix; Critical/Protected/RequiredKeep — never removable). High-confidence real CBS classified (Licenses/Kernel/FodMetadataServicing Critical+Protected+RequiredKeep; OneCore-DirectX kept for Gaming; SenseClient/Hello Security; VBSCRIPT Legacy; OpenSSH Client ProfileDependent; Notepad Productivity; Wallpaper Moderate consumer). Embedded lockdown/filter/UWF features: Enterprise/High/RecommendedKeep — never automatic Gaming removal. Catalog 145→177 entries; ZERO heuristic entries added. Gaming policies keep ALL languages. **1030 automated tests pass (Core 53, App 977), 0 errors, 0 warnings (Release, ordinary in-place)**. SECOND elevated RealCapture REQUIRED for exact new metrics (Unknown expected to fall materially from 524). **STAGE 14.3c — IMPLEMENTATION READY (2026-08-14):** SECOND elevated RealCapture run SUCCEEDED (real Administrator) — EXACT numbers: Total 757 · Curated 32 · Protected 51 · KnownDeep 591 · Heuristic 0 · **Unknown 134 · coverage 82.30%** (AppX 47: 22/3/10/15 · Capability 425: 2/3/348/75 · CBS 149: 0/41/148/1 · OptionalFeature 136: 8/4/85/43) — **Stage 14.3b VALIDATED + COMPLETE**. FINAL high-confidence long-tail pass (ADR-092): Wi-Fi/Ethernet driver capability families by VENDOR-FAMILY rule (Networking/High/RecommendedKeep/Sensitive — never auto-removed by Gaming); critical system items classified (DirectX.Configuration.Database RuntimeDependency/Critical/RequiredKeep/GamingRelevant; Microsoft.SecHealthUI Security/Critical/Protected/RequiredKeep; Microsoft-Windows-FodMetadata-Package Servicing/Critical/Protected/RequiredKeep; Microsoft.Onecore.StorageManagement SystemCore/High/Keep; Hello.Face Security/High/ProfileDependent/Sensitive); 7 media codec AppX (Media/Low/ProfileDependent — Gaming PC never auto-strips codecs, optional-only); user-facing AppX (Outlook/Office Hub Low+ConsumerContent — Gaming PC auto when supported AppX removal exists, gate blocks when unsupported; Dev Home Developer/ProfileDependent/DeveloperTool — Developer profile KEEP override, Gaming optional-only; ApplicationCompatibilityEnhancements SystemCore/High/RecommendedKeep, both AppX + CBS); clear capabilities (Console.Legacy/WebDriver/MathRecognizer/App.WirelessDisplay.Connect ProfileDependent; Wallpapers.Extended optional consumer); high-confidence features (ClientForNFS/DataCenterBridging/ADAM-Client/HostGuardian/LegacyComponents — ProfileDependent, never Low-risk auto). Dev Home added to the curated catalog (23 AppX definitions; AppX curated 22→23 on next capture). Deep catalog 177→203 (+27, zero heuristic). NO broad fallback rules (namespace-prefix test). **1105 automated tests pass (Core 53, App 1052), 0 errors, 0 warnings (Release, ordinary in-place)**. FINAL THIRD elevated RealCapture REQUIRED for exact new metrics (expected Unknown to fall materially from 134). **PHASE 14 — COMPLETED + MERGED TO `main` via `--no-ff` (2026-08-14):** THIRD elevated RealCapture run SUCCEEDED (real Administrator) — FINAL AUTHORITATIVE EXACT numbers: Total **757** · Curated **33** · Protected **53** · KnownDeep **645** · Heuristic **0** · Unknown **79** · **knowledge coverage 89.56%** across the currently supported real-media Component Intelligence discovery providers (AppX 47: 23 curated/4 protected/21 known/3 unknown · Capability 425: 2/3/385/38 · CbsPackage 149: 0/42/149/0 → **CBS 149/149 = 100% known** · OptionalFeature 136: 8/4/90/38). Provider scope: AppX/Capability/OptionalFeature/CbsPackage supported; Service/ScheduledTask/Driver/Language/WinRecovery/SystemApp NotSupported — **89.56% is semantic knowledge coverage across the SUPPORTED providers, NOT "89.56% of all Windows components"**. Real validation history: 30.78% (KnownDeep 201, Unknown 524) → 82.30% (591/134) → **89.56% (645/79)** — evidence of the family-based classification architecture on real 25H2 media. The remaining **79 Unknown entries are ACCEPTED as explicit technical debt** (ADR-093): CBS complete, AppX long-tail near-complete (3), remaining Capability/OptionalFeature debt is low-frequency long-tail (mostly singletons — Quick Assist/CrossDevice, MSIX tooling, MSMQ, MultiPoint, NFS admin, legacy IrDA/RIP, RSAT subfeatures, printing subfeatures, Recall, misc enterprise/legacy); Unknown is visible, honest debt — zero Unknown is NOT a product requirement; NO broad catch-all classifier was added (forbidden vanity patterns remain forbidden). **Gaming Profile 2.0 ACCEPTED**: Gaming PC (safe personal gaming optimization, retains normal Windows usefulness) vs Dedicated Gaming (more minimal OPTIONAL recommendations, health/compatibility-first, not kiosk); safety confirmed — Protected/Critical/High never auto-remove, heuristic never auto, Known ≠ Removable, GamingRelevant ≠ SafeToRemove, dependency/extras keeps final, manual overrides authoritative, NO placebo gaming tweaks, Defender enabled, Windows Update enabled, servicing protected, Store/Gaming Services/runtime/DirectX protected where required. Future work (Service/Driver/ScheduledTask/SystemApp/WinRecovery discovery, deeper dependency resolution, destructive CBS/driver removal execution, aggressive Lightweight/Dedicated execution, FullHealthValidated after deeper destructive customization) moved OUT of Phase 14 to subsequent phases (NO Stage 14.4). **1105 automated tests pass (Core 53, App 1052), 0 errors, 0 warnings (Release, ordinary in-place; pre- and post-merge)**. Merge: `--no-ff` to `main` (Phase 14 closeout, below); Phase 14 branch retained. **Stage 13 preflight UX simplified (2026-08-12):** retired the standalone compatibility card (still not visible on real desktop); compatibility is now ONE compact row inside the existing 「Windows information」 section — 「Windows 11 25H2 专业版 · x64 · WIM · ✓ 支持」. Media-level facts show immediately after Detect BEFORE edition selection (no SelectedEdition dependency); selecting an edition appends the localized edition name and refreshes instantly. Status marks honest: ✓ 支持 (workflow-validated baseline, never 完整验证通过), ⚠ 尚未完整验证 (future), ⚠ 仅检查支持 (ESD/SWM), ✕ 当前不支持 (blocker + red reason). Dead CompatibilityDetailsText removed — one model/one status/one rendering. **808 automated tests pass (Core 53, App 755), 0 errors, 0 warnings (Release)**. Baseline VM validation record (25H2 Pro zh-CN) still PENDING — required before Phase 13 close. **PHASE 12 — COMPLETED (2026-08-12): REAL DESKTOP VALIDATION — PASSED on Windows 11 25H2.** Confirmed end-to-end: workspace root change to F://WinForgeWorkspaces honored by ALL new workflows; old C: KnownRoot discoverable for cleanup but never reused for creation; shadow C:/F: split fixed; Prepare/Discard/Apply/Build/Finish all verified; Review validation UX + normalization/dedupe + true-conflict blocking; Hide Widgets Dsh-policy fix; Apply partial-failure reporting; Build→Completed→Finish enabled immediately; Finish cleanup removes the completed workspace, preserves the final ISO, DISM reports no mounted images; Storage detects legacy/old-root workspaces and shows each candidate's owning root; repeated workflows no longer leak ~6.8 GB per run. Original incident (~30 stale workspaces + temp ≈ 249 GB) RESOLVED by the Phase 12 lifecycle architecture. Non-blocking follow-ups recorded (ADR-072): long-run periodic disk-space checks less comprehensive than pre-op checks; recoverable-checkpoint minimization; conservative startup auto-cleanup (Storage/Finish/Discard cover it); Finish cleanup synchronous wait during large deletes; Custom profile + Extra Scenarios polish (Phase 11 ADR-061). **MERGED TO `main` via `--no-ff` (2026-08-12).**
- 2026-08-12 — **PHASE 11 CLOSEOUT — COMPLETED / REAL DESKTOP VALIDATED / MERGED TO `main`.** Stage 11.4 passed real-desktop validation on the Windows 11 25H2 zh-CN x64 Consumer image (profile selection + immediate safe application, selected count, override protection, detail overlay open/return, state preservation, Gaming/Developer/Lightweight meaningful differences, usable layout with visible list, all six tabs + Review, capability OpenSSH handling; no major binding/runtime defects). Final in-place `dotnet build/test WinForge.sln -c Release` = **0 errors, 0 warnings, 662 pass (Core 53, App 609)**; closeout docs committed on `phase/11-component-intelligence`, then **merged to `main` with `--no-ff`** (merge commit below); main rebuilt + retested green and pushed. One NON-BLOCKING follow-up recorded (ADR-061): Custom profile currently disables Extra Scenarios — future polish allows extras in Custom mode as keep/recommendation hints without a primary preset. Phase 11 COMPLETE.
- 2026-08-12 — **Phase 11 Stage 11.3 OpenSSH CORRECTION (ADR-056) — IMPLEMENTED (`phase/11-component-intelligence`, NOT merged to `main`).** OpenSSH Client/Server are Windows CAPABILITIES per Microsoft docs (`OpenSSH.Client~~~~0.0.1.0` / `OpenSSH.Server~~~~0.0.1.0`, Get/Add-WindowsCapability), never OptionalFeature FeatureNames. Catalog `TechnicalTargets` switched to `Category = Capability` + exact capability identities + mechanism `RemoveCapability`; both names removed from `FeatureConfigPolicy.AllowedFeatureNames`; rows stay VISIBLE when present with disabled checkbox + "当前版本暂不支持应用" (`Opt.ApplyUnsupported`) and selection is a no-op (no silent-skip Apply operation). Regression test proves resolution through the Capability inventory only (a feature-shaped "OpenSSH.Client" raw item must NOT match). Docs/report wording corrected (CHANGELOG/ARCHITECTURE/ROADMAP/DECISIONS ADR-056/coverage matrix). In-place `dotnet build/test WinForge.sln -c Release` = **0 errors, 0 warnings, 591 pass (Core 53, App 538)**. Stage 11.3 — PENDING REAL DESKTOP REVIEW; Phase 11 IN PROGRESS; NOT merged to `main`.
- 2026-08-11 — **Phase 11 Stage 11.3 real-desktop DEFECT FIX — Windows Components tab zero rows (`phase/11-component-intelligence`, NOT merged to `main`).** Root cause (ADR-055): `CustomizeStepViewModel.DiscoverAllAsync()` only refreshed the Apps knowledge VM; the Windows Components knowledge VM (same CI inventory, capability/optional-feature filter) never re-ran `Rebuild()` after the unified Discover, so it stayed in the pre-discovery empty state ("请先发现当前映像中的组件。", zero rows). Fix: `ComponentKnowledgeViewModel.RefreshFromInventory()` + `DiscoverAllAsync()` refreshes BOTH knowledge tabs from one DISM pass; execution eligibility separated from display eligibility (`ComponentKnowledgeItem.IsApplySupported` + `FeatureConfigPolicy`) so capability / not-yet-allowlisted feature rows stay VISIBLE with a disabled checkbox and explicit "当前版本暂不支持应用" reason instead of being hidden or silently Skipped. 6 new regression tests (`Stage11p3ComponentsTabDefectTests`); full suite **591 pass (Core 53, App 538), 0 errors, 0 warnings (Release)**. Stage 11.3 — PENDING REAL DESKTOP REVIEW; Phase 11 IN PROGRESS; NOT merged to `main`.
- 2026-08-11 — **Phase 11 Stage 11.3 — Customize Coverage Expansion + Personalization Activation + Optimization Knowledge Matrix — IMPLEMENTED (`phase/11-component-intelligence`, PENDING REAL DESKTOP REVIEW; NOT merged to `main`).** Coverage-first expansion per ADR-051..054: (1) a coverage matrix (`.tmp/phase11/stage11.3-coverage-matrix.md`) audits every candidate with 13 fields + status Implemented/Deferred/Rejected/Unsupported and a reason (VERIFIED / COMMUNITY_REFERENCE / UNKNOWN policy; community never auto-promotes). (2) Operation taxonomy: `OptimizationAction`/`OptimizationMechanism`/`OptimizationScope` carried as data on `CustomizationOperation`; new `DisableOptionalFeature` + `RemoveCapability` operation types validated in the plan and executed via DISM behind a `FeatureConfigPolicy` allowlist; `CustomizationCategory.Personalization`; `FreezeForExecution` preserves the new fields; Review now lists every selected change with exact action type + scope + revert contract. (3) Offline registry/Default-User targeting: `OfflineHivePaths.DEFAULT_USER` → `<mount>\Users\Default\NTUSER.DAT` (host HKCU never touched); per-value `RestoreValueData` reversibility. (4) Knowledge reuse (Part L): the Windows Components tab reuses `ComponentKnowledgeViewModel` with a capability/optional-feature filter over the composite catalog; Services/Privacy/System/Personalization share ONE `OptimizationKnowledgeViewModel`/`OptimizationKnowledgeView` over the generated `OptimizationCatalog` — first tranche counts: Windows Components 12 · Services 12 (11 reviewed + RpcSs informational) · Privacy 11 · System 10 · Personalization 14 (Start/Search + Taskbar + Explorer + Lock screen/Desktop + Appearance; the Experience/Coming Soon tab is replaced — Personalization no longer displays Coming Soon). Service allowlist extended with reviewed services only. 28 new `Stage11p3Tests` (content validation, no Unknown leak, host-safety, mechanism→op mapping, build/edition gating, post-install blocking, core-service blocking, Review action types, reversal round-trip, DISM feature-disable execution, capability skip); full suite **584 pass (Core 53, App 531), 0 errors, 0 warnings (Release)**. Docs updated (PROJECT_STATUS/ROADMAP/ARCHITECTURE/DECISIONS ADR-051..054). STAGE 11.3 — PENDING REAL DESKTOP REVIEW; Phase 11 remains IN PROGRESS; NOT merged to `main`.
- 2026-08-11 — **Phase 11 Stage 11.2 real-desktop DEFECT FIX — IMPLEMENTED + PENDING REAL DESKTOP REVIEW (`phase/11-component-intelligence`, NOT merged to `main`).** Real-desktop validation of the ADR-048 rework (commit `c341926`) found the Customize → Apps tab rendered incorrectly (invisible list + large empty detail panel). Three root causes fixed (ADR-049): (1) **Data** — `ComponentKnowledgeViewModel.Rebuild()` now filters to `Curated && RawItems.Count > 0`, so only curated components PRESENT in the image appear (catalog-only/absent definitions are excluded; before discovery the list is empty). (2) **Layout** — `ComponentKnowledgeView` restructured to a two-column layout (list + empty-state in Col 0, detail side panel in Col 1); the detail `ContentControl` is `Collapsed` (`NullToVis` on `ActiveDetail`) when no detail is selected, so it can never squeeze the list to zero height; an explicit empty-state (`Knowledge.EmptyAwaitDiscovery` / `Knowledge.EmptyNoCurated`) replaces any empty detail card. (3) **Discovery UX** — `CustomizeStepViewModel.DiscoverCommand` is now a unified `AsyncRelayCommand` running BOTH `Components.DiscoverAsync()` and `_knowledge.DiscoverAsync()` (CI knowledge discovery); the duplicate in-tab Discover button is removed — one button, one coherent read-only pass (no destructive duplication). The "white/invisible" labels were a symptom of the null-DataContext detail panel (resolved by the collapse). 8 new + 7 updated regression tests; full suite **542 pass (Core 53, App 489), 0 errors, 0 warnings (Release)**. Stage 11.2 PENDING REAL DESKTOP REVIEW; Phase 11 remains IN PROGRESS; NOT merged to `main`.
- 2026-08-11 — **Phase 11 Stage 11.2 master–detail rework — IMPLEMENTED + PENDING REAL DESKTOP REVIEW (`phase/11-component-intelligence`, NOT merged to `main`).** Product decision: remove the per-row "详情 / Details" button; make any row click open/switch the right-side detail panel (ADR-050). Changes: (1) `ComponentKnowledgeView` row `ItemTemplate` no longer renders a Details `Button`; the standard Apps row is exactly 选择 | 名称 | 作用 | 建议 | 风险 (the only remaining button is the detail panel's × close, outside the list). (2) `ListView.MouseLeftButtonUp` + `KeyDown` route to `ShowDetailCommand`; clicks originating on the `CheckBox` are ignored (`IsWithinCheckBox`) so removal selection stays independent; Enter on a focused row opens/switches detail, Space stays on the checkbox. (3) Two states never conflated — `ActiveDetail` (inspection) vs `RemovalSelected` (plan); `ActiveDetail` setter refreshes each row's `IsActiveDetail` flag (distinct `#E8F0FE` background highlight, separate from the checkbox checked state); closing detail never touches selections. (4) `ApplyFilter()` closes `ActiveDetail` when the open item leaves the visible filtered set (selection survives); blocked rows keep an inspectable detail (block reason shown via new `StringNullOrEmptyToVisibilityConverter`); `VirtualizingStackPanel.IsVirtualizing="False"` + `ScrollViewer.HorizontalScrollBarVisibility="Disabled"` guarantee no horizontal scroll at normal width. 14 new regression tests (details button removed; row click opens/switches detail; panel stays open; row click does not change removal; checkbox changes removal only; checkbox does not change detail; checked survives inspect; × clears detail only; selections survive close; active highlight distinct; blocked row opens detail; blocked checkbox disabled; filter + detail; active closes when filtered out; Enter opens detail; Enter does not toggle removal; no horizontal scroll); full suite **556 pass (Core 53, App 503), 0 errors, 0 warnings (Release)**. Stage 11.2 PENDING REAL DESKTOP REVIEW; Phase 11 remains IN PROGRESS; NOT merged to `main`.
- 2026-08-10 — **Phase 11 Stage 11.2 UX REWORK — IMPLEMENTED + PENDING REAL DESKTOP REVIEW (`phase/11-component-intelligence`, NOT merged to `main`).** Real-desktop review of Stage 11.2 found the separate Customize "组件知识 / Component Knowledge" tab redundant and poorly presented (it duplicated the Component Intelligence data in a second, lower-quality surface and forced the user to leave the component list to decide removal). Rework (ADR-048): (1) the knowledge engine (`ComponentKnowledgeViewModel`/`ComponentKnowledgeView`/`ComponentKnowledgeItem`) is **reused, not deleted** — it is repurposed as the Customize **Apps tab** (passed as the tab `Content`; App.xaml's implicit `DataType` DataTemplate renders it, no duplicate View/ViewModel); the former `CustomizeTabKind.Knowledge` enum value and `CustomizeStepViewModel.Knowledge` property are removed. (2) The Apps row is 选择 | 名称 | 作用 | 建议 | 风险 + 详情 + 阻塞原因 (category dropped from the row, kept in hover/detail); raw Windows package identity (`Microsoft.*_..._8wekyb3d8bbwe`) is hidden from the row and hover card and shown **only** in the collapsed detail / Advanced / the CI page. (3) The left-rail 组件智能/Component Intelligence page is repositioned as **高级组件检查器 / Component Inspector** (advanced, raw inspection). (4) Decision-oriented relabeling: 推荐精简 / 按需精简 / 建议保留 / 高级选项 / 不可移除 (+ en). (5) Progressive integration (no fake intelligence); `ScenarioRecommendation` preserved for future Profiles. 25 new/updated regression tests guard it; full suite **534 pass (Core 53, App 481), 0 errors, 0 warnings (Release)**. Stage 11.2 UX REWORK IMPLEMENTED; PENDING REAL DESKTOP REVIEW; Phase 11 remains IN PROGRESS; NOT merged to `main`.
- 2026-08-10 — **Phase 11 Stage 11.2 — Component Knowledge Import + Catalog Expansion + Customize Integration — IMPLEMENTED (`phase/11-component-intelligence`, PENDING REAL DESKTOP REVIEW).** Builds on Stage 11.1's read-only inventory to make curated knowledge actionable for ordinary users without any destructive automation. (Part A) A knowledge-provenance model deliberately separates `KnowledgeClaimKind.Fact` from `Recommendation`; `KnowledgeSource` (Curated / MicrosoftOfficial / WindowsImageDiscovery / Community) and `ScenarioRecommendation` carry per-claim provenance so a community removal script can never become a WinForge "RecommendedRemove". (Part B) An offline `KnowledgeImportPipeline` ingests `IKnowledgeSourceAdapter`s (MicrosoftOfficial / WindowsImageDiscovery / Win11DebloatCommunity / WinForgeCurated); candidates **never** auto-promote to Curated and a community `CommunityProposal` is never promoted to `EffectiveRecommendation` (it stays informational). (Part C) `gen_catalog.py` is now **idempotent** — it owns every `SHARED` + `Comp.*` + `Prov*` + `Scen.*` resx key and strips+rewrites them on each run, so re-running produces exactly 284 keys with **no duplicate-key warnings** (was 200–332). (Part D–H) Catalog grew **11 → 22** well-understood components (AV1/AVC video extensions, Bing News/Search, Calculator, Notepad, Paint, Terminal, To Do, Quick Assist, Desktop App Installer, and an "Xbox / Gaming" grouping of 9 AppX identities with a Gaming→UsuallyKeep scenario override); `CuratedComponentCatalog` regenerated. (Part L) `ComponentIntelligenceViewModel` is the shared knowledge engine; `ComponentKnowledgeViewModel` reuses its classified inventory (single discovery). (Parts D–L) A Customize "Component Knowledge" tab (`ComponentKnowledgeViewModel`/`ComponentKnowledgeItem`/`ComponentKnowledgeView`) presents human name/category/recommendation/risk badges, a default usefulness sort, filters, a compact hover quick card, click-for-detail without changing selection, conservative Protected/Unknown UX (explicit block reasons), official-vs-community evidence, and deterministic "why" captions — with **no automatic destructive selection**; Component Intelligence remains the advanced inspection surface. **530 automated tests pass (Core 53, App 477), 0 errors, 0 warnings (Release).** ADR-045/ADR-046/ADR-047. NOT merged to `main`; Phase 11 remains IN PROGRESS; Stage 11.2 PENDING REAL DESKTOP REVIEW.
- 2026-08-10 — **Phase 11 Stage 11.1 — Component Intelligence — REAL DESKTOP VALIDATED (`phase/11-component-intelligence`, NOT merged to `main`).** Real Windows 11 25H2 zh-CN x64 Consumer `install.wim` desktop validation PASSED: Component Intelligence page renders correctly (the sandbox-traced blank-page defect is fixed on the real build); real mounted-image discovery succeeds; Standard mode shows only the 11 curated components; Advanced mode exposes real discovered raw Windows objects for inspection; raw unclassified objects are NOT presented as trusted/removable items; curated detail panel renders recommendation/risk/impact/restore/technical metadata. Real classification counts: **Curated 11 · DiscoveredUnclassified 734 · Protected 13 · Unsupported 0**. Representative real unclassified objects observed: `Microsoft.ApplicationCompatibilityEnhancements`, `Microsoft.AV1VideoExtension`, `Microsoft.AVCEncoderVideoExtension`, `Microsoft.BingNews`, `Microsoft.BingSearch`, `Microsoft.DesktopAppInstaller`, `Microsoft.GamingApp`. PRODUCT CONCLUSION: the 734 discovered objects must NOT become 734 normal removal checkboxes — they remain raw Windows identities, surfaced read-only in Advanced mode. Architecture goal confirmed: raw Windows identities are discovered independently from curated WinForge logical components. **491 automated tests pass (Core 53, App 438), 0 errors, 0 warnings (Release).** ADR-045/ADR-046. Stage 11.2 (NOT STARTED): Component Catalog Expansion — turn representative real discovered objects into user-understandable logical components (purpose/risk/keep-if/remove-if/impact/restore); start with high-value low-risk families; no deep CBS removal; Protected never exposed for removal; no inferred deps. Phase 11 remains IN PROGRESS; NOT merged to `main`.
- 2026-08-10 — **Phase 11 Stage 11.1 — Component Intelligence — IMPLEMENTED (`phase/11-component-intelligence`, PENDING REVIEW).** Evolves WinForge from "show package names" to "tell ordinary users WHAT a component is, WHETHER they need it, WHAT breaks if removed, HOW risky, whether restorable." Core `ComponentMatcher.BuildInventoryEntries` (pure, platform-agnostic, no DISM) separates the discovered Windows object (`IRawInventoryItem` → `RawAppxPackage`/`RawCapability`/`RawOptionalFeature`/`RawCbsPackage`) from the human `ComponentDefinition`, mapping raw items onto curated definitions and classifying the rest as Curated / DiscoveredUnclassified / Protected / Unsupported. Infrastructure adds four read-only DISM parsers (`AppxInventoryParser`/`CapabilityInventoryParser`/`OptionalFeatureInventoryParser`/`CbsPackageInventoryParser`) + `WindowsComponentIntelligenceService` orchestrator (tolerant per-source failure; 6 further categories — Service/ScheduledTask/Driver/Language/WinRecovery/SystemApp — reported `NotSupported`). Curated catalog of 11 well-understood inbox AppX (`CuratedComponentCatalog`, generated by `.tmp/phase11/gen_catalog.py`; Teams `Requires` OneDrive). App: `ComponentIntelligenceViewModel` (Standard mode = curated only, Advanced mode also raw; CanDiscover gated on mounted workspace), `ComponentListItem` (Unknown preferred over invented — localized `Component.Unknown` = "Unknown"/"尚未确认"), `ComponentIntelligenceView` detail prototype + `RecommendationToColorConverter`/`RiskToColorConverter`, additive `PageKey.ComponentIntelligence` rail entry (no Phase-10 behavior changed), new `Strings.resx`/`Strings.zh-CN.resx` keys. **491 automated tests pass (Core 53, App 438), 0 errors, 0 warnings (Release).** ADR-045/ADR-046. Real Windows 11 25H2 zh-CN x64 Consumer ground-truth enumeration **PASSED** (Curated 11 · DiscoveredUnclassified 734 · Protected 13 · Unsupported 0). PRODUCT CONCLUSION: the 734 discovered raw objects must NOT become 734 normal removal checkboxes — they remain raw Windows identities, surfaced read-only in Advanced mode; Stage 11.2 turns representative families into user-understandable logical components (Unknown stays Unknown until evidence-backed). NOT merged to `main`; Phase 11 remains IN PROGRESS.
- 2026-08-10 — **Phase 10 Build / ISO Export — FINAL CLOSEOUT (`feature/iso-build` → `main`).** Real-desktop validation PASSED on Windows 11 25H2 (Chinese Simplified, x64, Consumer Editions, `install.wim`): Build entered after Apply; working image committed + automatically unmounted; clean install.wim exported; media tree copied + payload replaced; ReadOnly/System/Hidden `autorun.inf` defect fixed (build copy clears those attrs on the **build copy only** — never the source — + deterministic media-tree cleanup); oscdimg dual-boot ISO built; independent verification passed; build reached 100% / Completed; Finish visible + enabled and navigates Workflow → Home; application stays running; generated ISO preserved; no extra manual dismount; no stale mounted WIM. Three real-desktop defect fixes landed on `feature/iso-build` before closeout: (a) `autorun.inf` ReadOnly/System/Hidden unlock (`cb0165a`, resumable post-commit checkpoint + deterministic cleanup); (b) final-step Finish UX polish — completion-gated localized Finish + Open-output-folder, hides the meaningless disabled Next (`912522c`, +7 regression tests); (c) Finish-navigation defect fix — wizard surface shown via `INavigationService.NavigateTo(PageKey.Workflow)` so `CurrentPage` stays in sync and Finish truly navigates Workflow → Home (`084dafb`, +13 shell-level integration tests; `AppBootTests` updated for new startup navigation log). Merged to `main` via `--no-ff` (merge commit `43e73a2`; feature history preserved, no squash). **440 automated tests pass (Core 37, App 403), 0 errors, 0 warnings (Release).** Phase 10 = **COMPLETED**; Desktop Validation = **PASSED**; merged to `main`. No Step 3.4 / Phase 4.
- 2026-08-10 — **Phase 10 Build / ISO Export — IMPLEMENTED (`feature/iso-build`).** Replaces the honest placeholder Build step with a real, safe ISO-rebuild pipeline. `ImageBuildService` orchestrates a 6-phase state machine (Preflight → CommittingImage → ExportingImage → PreparingMedia → BuildingIso → Verifying → Completed/Failed/Cancelled) delegating to `IImageServicingService.CommitUnmountAsync` (DISM `/Unmount-Image /Commit`; `/Discard` never used on the build path — ADR-039), `IWimExporter`/`DismWimExporter` (`DISM /Export-Image /Compress:max /CheckIntegrity`; clean install.wim; ESD normalized to WIM at index 1 — ADR-040), `IIsoMediaPreparer`/`IsoMediaPreparer` (read-only copy of the original media tree, replaces `sources\install.wim` or deletes `install.esd` + writes `install.wim`, validates dual-boot files — ADR-040), `IBootableIsoBuilder`/`OscdimgIsoBuilder`/`OscdimgArgumentBuilder` (Windows ADK `oscdimg.exe`, dual-boot args, fails fast + clearly when ADK missing or a boot file absent, never fakes an ISO — ADR-041), `IBuildVerifier`/`BuildVerifier` (independent re-check; failed verification fails the build — ADR-043), `IAdkToolLocator`, and `BuildStepViewModel` (gated `CanBuild`, default `WinForge_<Edition>_<yyyyMMdd-HHmm>.iso`, explicit overwrite policy default `GenerateUniqueName`, cancellable command, terminal state + log + output surfaced from `BuildResult` — ADR-042). `ImageBuildService` writes an atomic `build.recovery.json` for crash recovery (ADR-043). Fixes during the suite: duplicate `FakeFilePicker` removed; xUnit2013 warnings cleared; `DefaultIsoName` normalizes edition spaces to `_`; `AsyncRelayCommand` gained an awaitable `ExecuteAsync` (the custom MVVM command had only `async void Execute`); `BuildStepViewModel` pins `CurrentStage`/`LogText` from the result so async progress delivery cannot leave the UI mid-state. **397 automated tests pass (Core 37, App 360), 0 errors, 0 warnings (Release).** Status: **IMPLEMENTED / PENDING REAL DESKTOP VALIDATION**; not merged to `main`; ADR-038…ADR-043 added.
- 2026-08-09 — **Phase 3.5 Wizard/Localization — REAL DESKTOP VALIDATED + CLOSEOUT (`feature/wizard-localization` → `main`).** Real Windows 11 25H2 (Chinese Simplified, x64, Consumer Editions, `install.wim`) desktop run of the full Wizard workflow PASSED all 16 acceptance points: Source ISO inspection; Prepare isolated working image; Mount working WIM; Mounted→Next enables; Customize loads; all Customize tabs load with no XAML/binding crash; runtime zh-CN localization works; discovery + selections work; selecting 1 valid customization enables Review/Next; Review loads; plan validates; Apply to mounted image succeeds; execution success advances Wizard state (Review→Completed, Apply→Available, Next→enabled); Build placeholder reachable; Build/ISO export intentionally NOT implemented (honest placeholder); working image cleanly unmounted/discarded (no mounted WIM remains). The feature branch carried 7 real-desktop defect fixes (`28b8bb5` servicing nested notification, `7f90d48`/`4360612` ISO mount robustness, `06de8f9` `CustomizationPlan` nested notification, `f1e4370` `Run.Text` OneWay, `9f68f97` read-only binding OneWay audit, `bed2e73` `ErrorDialogGuard`, `7da38dd` Review/Apply gating). Merged to `main` via `--no-ff` (feature history preserved, no squash); **362 automated tests pass (Core 37, App 325), 0 errors, 0 warnings (Release)**. Wizard/Localization = **IMPLEMENTED / REAL DESKTOP VALIDATED / COMPLETED**. Next phase: Build/ISO Export (NOT STARTED).
- 2026-08-09 — **UX Workflow Refactor + Localization Foundation — IMPLEMENTED (`feature/wizard-localization`, branch `wf-wizard`).** Replaces the left feature-list nav with a 6-step Wizard/Stepper (Source → Prepare → Customize → Review → Apply → Build; zh: 选择镜像 → 准备镜像 → 自定义 → 审核计划 → 应用修改 → 构建镜像) as the primary surface; utility pages (Home/Logs/Settings/About) moved to a separate rail (ADR-032/ADR-033). Localization foundation: neutral `Strings.resx` + `Strings.zh-CN.resx` satellite (parity tested), `ResourceManagerLocalizationService` exposed as `Loc`, `LocKeyMultiConverter` so bindings re-evaluate on key + culture change, `ILocalizationService` in Core, runtime `SetCulture` with `ILanguageSettingsStore` persistence and English fallback (ADR-034/ADR-035). `FriendlyMetadataProvider` maps a trusted allowlist (DiagTrack/WerSvc/PcaSvc; Microsoft.BingWeather/GetHelp/WindowsCamera/XboxApp) to localized `.resx` keys while preserving the immutable technical id everywhere; `ServiceConfigPolicy` still gates which services are configurable (ADR-036). `ComponentsViewModel` selection→plan resync guarded by `_suppressPlanResync` so a toggle no longer self-cancels (ADR-037). Customize tabs: Apps / Windows Components / Services / Privacy / System / Experience; Review reshapes the Plan; Apply is the execution UX; Build is an honest placeholder (no fake ISO rebuild). **49–50 new tests** across WORKFLOW / COMMANDS / LOCALIZATION / SERVICE / APP / REGRESSION; total suite **312 pass (Core 37, App 275), 0 errors, 0 warnings (Release)**. Status: **IMPLEMENTED / PENDING REAL DESKTOP VALIDATION**; not merged to `main`; ADR-032…ADR-037 added.
- 2026-08-09 — **Phase 3 Step 3.3 DESKTOP VALIDATION PASSED + closeout (`feature/offline-customization` → `main`).** Real Windows 11 25H2 (Chinese Simplified, x64, Consumer Editions, `install.wim`) desktop re-run of the offline customization engine PASSED: ISO inspection + edition select → Ready; Prepare exported source index → isolated working WIM; Mount succeeded (real mount contained Windows / Program Files / Users / PerfLogs); provisioned-Appx discovery returned 47 packages with exact `PackageName` identity (independent DISM inventory confirmed); offline service discovery returned 699 services with only DiagTrack/WerSvc/PcaSvc exposed as configurable (696 protected/driver entries hidden, no silent-zero); non-allowlisted packages (e.g. `Microsoft-OneCore-ApplicationModel-Sync-Desktop-…`) non-selectable; selected plan (remove `Microsoft.BingWeather`, disable `DiagTrack`, turn off advertising ID) → Validate passed → Apply → 3 succeeded, 0 failed; independent DISM confirmed `Microsoft.BingWeather` removed; offline `SYSTEM` hive confirmed `DiagTrack\Start = 0x4`; offline `SOFTWARE` hive confirmed `AdvertisingInfo\Enabled = 0x0` (ADR-031 corrected path + read-back contract verified); verification hives `WF_VERIFY_SYSTEM`/`WF_VERIFY_SOFTWARE` unloaded cleanly; unmount reported "No mounted images found"; original ISO / `install.wim` untouched. 259/259 automated tests pass (Core 37, App 222), 0 errors, 0 warnings (Release). Step 3.3 = **COMPLETED**; Desktop Validation = **PASSED**; ready to merge to `main`. No Step 3.4 / Phase 4.
- 2026-08-09 — **Phase 3 Step 3.3 offline registry write success contract (`feature/offline-customization`) — ADR-031.** Real-desktop defect: a 3-operation plan (remove BingWeather, disable DiagTrack, turn off advertising ID) reported "3 succeeded, 0 failed", yet independent `reg.exe` verification found the advertising-ID value **absent** from the offline `SOFTWARE` hive. Root causes: (1) the trusted definition `privacy.advertising-id` targeted `Microsoft\Windows\CurrentVersion\Advertising\Id` but the real Windows key is `Microsoft\Windows\CurrentVersion\AdvertisingInfo` (value `Enabled` directly under it) — the write *succeeded at the wrong location*; (2) `ApplyRegistry` returned `Succeeded` merely because `SetValue`/`DeleteValue` did not throw, with **no post-write read-back verification**. The hypothesized duplicated `SOFTWARE\SOFTWARE` prefix was investigated and ruled out (the write chain is relative to the loaded hive root, `HKLM\WinForge_SOFTWARE`), but a guard was added anyway. Fixes (no merge to `main`, validation not marked passed): (1) definition corrected to `Microsoft\Windows\CurrentVersion\AdvertisingInfo` (all 5 Privacy + 3 System definitions audited — hive-prefix consistent, no `SOFTWARE\` prefix); (2) after every `SetOfflineRegistryValue` the engine read-backs via `OfflineRegistryService.ReadValue` and confirms existence + requested type + requested value, else `FailedRecoverable`; `DeleteOfflineRegistryValue` confirms absence; the production service self-verifies too (defense-in-depth); (3) new `OfflineHivePaths.NormalizeKeyPath` strips a leading `HKLM\` / hive-base segment so a path is always strictly relative to the loaded hive root; (4) host isolation preserved (`OfflineHivePaths` + `IMountIdentityValidator`; unknown/absolute hive base like `HKLM` rejected). 17 new regression tests (`OfflineRegistryContractTests`); 259/259 automated tests pass (Core 37, App 222), 0 errors, 0 warnings (Release). Step 3.3 remains **PENDING real-desktop validation (re-run)**; not merged to `main`; no Step 3.4 / Phase 4.
- 2026-08-09 — **Phase 3 Step 3.3 service-inventory safety boundary (`feature/offline-customization`) — ADR-030.** Real-desktop re-validation confirmed the GOOD results (47 Appx, 149 packages, 699 services; non-approved packages correctly disabled) but surfaced a NEW safety defect: the Components page exposed a disableable checkbox for **every one of the 699 discovered service records** — including kernel/file-system drivers and performance/provider entries (`.NET CLR Data`, `.NET Data Provider for Oracle`, `.NET Memory Cache 4.0`, …). Prior code classified every service `RiskClass.Removable` unconditionally. Fixes (no merge to `main`, validation not marked passed): (1) new `ServiceClass` enum (Unknown/Driver/Protected/Configurable/RecommendedConfigurable) and new `ServiceConfigPolicy` single source of truth (only `DiagTrack`/`WerSvc`/`PcaSvc` configurable; a test pins it to `CustomizationDefinitionProvider`'s recommended changes); (2) `DiscoverServices` now reads each service `Type` — driver types → `Driver` (protected), allowlisted Win32 → `RecommendedConfigurable`, else `Protected`; (3) `ServiceSelectionItem.CanSelect` driven by `ServiceClass` with short reasons; `ComponentsViewModel` shows only configurable services by default with a `ShowProtectedEntries` read-only toggle; (4) three-layer guard — `PlanSync` refuses unapproved service ids, `CustomizationPlan.ClassifyBase` flags them `Unsupported`, `WindowsCustomizationExecutionService.ApplyService` retains a final `Skipped` guard; host SYSTEM-hive boundary intact. 12 new regression tests; 242/242 automated tests pass (Core 37, App 205), 0 errors, 0 warnings (Release). Step 3.3 remains **PENDING real-desktop validation (re-run)**; not merged to `main`; no Step 3.4 / Phase 4.
- 2026-08-09 — **Phase 3 Step 3.3 Appx removal-identity / fixture-fidelity RE-INVESTIGATION (`feature/offline-customization`) — ADR-029.** Independently reproduced DEFECT 1 on a real mounted image: `dism /English /Get-ProvisionedAppxPackages` SUCCEEDS and returns many packages, yet WinForge reported "Discovered 0 app(s)". Confirmed the live `DismAppxParser` already accepts the real single-word `PackageName` header (the Step 3.3 report's "Deployment package name" was the invented synthetic key); the mismatch lived in the doc comment, the unit fixtures, and historical ADR text. Fixes (no merge to `main`, validation not marked passed): (1) parser doc comment corrected to the real `PackageName`/`DisplayName` headers and the end-to-end removal identity (exact `PackageName`, `DisplayName` display-only, a block without `PackageName` dropped); (2) `DismAppxParserTests.Sample` and `WindowsCustomizationDiscoveryServiceTests.AppxOut` replaced with REAL DISM output copied from the desktop test (Clipchamp/BingWeather/Windows.Photos, `DisplayName` then `PackageName`); (3) explicit four-way outcome contract — valid+found → `Success(N)`, valid+genuine-zero → `Success(0)` (legitimate), command failure → `Failed`, unrecognized output → `Failed`. New tests `RemovalIdentity_IsExactPackageName_NotDisplayName`, `IsRecognizedOutput_True_ForGenuineZeroWithBanner`, `Discover_GenuineZeroAppx_IsSuccess_NotFailed`. 224/224 automated tests pass (Core 37, App 187), 0 errors, 0 warnings (Release). Step 3.3 remains **PENDING real-desktop validation (re-run)**; not merged to `main`; no Step 3.4 / Phase 4.
- 2026-08-09 — **Phase 3 Step 3.3 offline-services silent-zero RE-INVESTIGATION (`feature/offline-customization`) — ADR-028.** Real-desktop evidence confirmed the SYSTEM hive file exists and is readable (`<mount>\Windows\System32\Config\SYSTEM`, 9,175,040 bytes), so the services=0 cause was **not** a missing/wrong hive path. The residual silent-zero path was a *successfully loaded* hive whose resolved `ControlSet00x\Services` enumeration returned empty, which previously returned `Success` with 0 items. Fixes (no merge to `main`, validation not marked passed): (1) `DiscoverServices` now treats an empty Services enumeration as `ServiceStatus = Failed` (never silent "0 services"); (2) `OfflineRegistryService` now depends on `ILoggerService` and logs the full load/unload lifecycle — redacted hive path, WinForge-owned temp HKLM name, `SeRestore`/`SeBackup` enablement incl. `ERROR_NOT_ALL_ASSIGNED`, `RegLoadKey`/`RegUnLoadKey` return codes, resolved ControlSet, service count — with the mount-root prefix redacted and no host-registry data logged (host-safety boundary intact). New regression test `Discover_SurfacesEmptyServicesEnumeration_AsError_NotSilentZero`. 221/221 automated tests pass (Core 37, App 184), 0 errors, 0 warnings (Release). Step 3.3 remains **PENDING real-desktop validation (re-run)**; not merged to `main`; no Step 3.4 / Phase 4.
- 2026-08-09 — **Phase 3 Step 3.3 real-desktop validation DEFECT FIXES (`feature/offline-customization`) — ADR-026/ADR-027.** Fixed three real-desktop defects without merging to `main` and without marking validation passed: (DEFECT 1) `DismAppxParser` now matches the real DISM single-word `PackageName`/`DisplayName` headers and `RunDismAsync` checks DISM exit code + stderr and rejects unrecognized/localized output; (DEFECT 2) `OfflineRegistryService` enables `SeRestorePrivilege`/`SeBackupPrivilege` before `RegLoadKey`/`RegUnLoadKey` and `DiscoverServices` surfaces hive-load failure as `ServiceStatus=Failed` instead of silent zero; (DEFECT 3) `PackageRemovalPolicy` is now the single policy source governing discovery classification, plan validation (`PlanSync`/`RecomputeValidation`), and execution defense-in-depth, so no non-allowlisted package can be selected. `DiscoveryInventory` gained per-source status/error. 220/220 automated tests pass (Core 37, App 183), 0 errors, 0 warnings (Release). Step 3.3 remains **PENDING real-desktop validation (re-run)**; not merged to `main`; no Step 3.4 / Phase 4.
- 2026-08-09 — **Phase 3 Step 3.3 IMPLEMENTED (`feature/offline-customization`) — offline customization plan & execution engine.** Adds a declarative, platform-agnostic `CustomizationPlan` (Core: lifecycle Draft→Validated→Executing→Completed/CompletedWithErrors/Failed/Cancelled; `Validate()` surfaces Duplicate/Conflict/Unsupported/MissingTarget; `FreezeForExecution()` snapshots + locks) and `CustomizationOperation`/`CustomizationResult`/`DiscoveryInventory` + typed enums. Infrastructure: `DismAppxParser` (exact-identity provisioned Appx), `DismPackageParser` (allowlist-gated `Removable` packages), `OfflineRegistryService` (Win32 `RegLoadKey`/`RegUnLoadKey` under `HKLM\WinForge_<BASE>`, always unloaded in `finally`), `MountIdentityValidator` (path confinement + session binding), `WindowsCustomizationDiscoveryService` (loads SYSTEM hive to enumerate services, merges trusted definitions, refuses on session mismatch), `WindowsCustomizationExecutionService` (pre-run critical-stop guard → `CriticalFailure`; frozen ordered execution registry→services→appx→packages→files→tasks; package allowlist skip; leaves image mounted), and `CustomizationDefinitionProvider` (5 Privacy + 3 System registry settings, 3 recommended service-start changes). App: Components/Privacy/System/Plan pages backed by discovery, selection→plan ops via `PlanSync`, explicit Validate+Apply; DI + navigation wired (`Plan` PageKey + 4 DataTemplates). Safety: every operation confined to the mounted working image; no host path / source-ISO root / arbitrary command/registry/fs delete; mount identity verified; session mismatch refused (ADR-020–ADR-025). Governance updated: ROADMAP Step 3.3, DECISIONS ADR-020–ADR-025, ARCHITECTURE offline-customization section, this status, CHANGELOG. 206/206 automated tests pass (Core 37, App 169), 0 errors, 0 warnings (Release). Step 3.3 = **IMPLEMENTED / PENDING REAL-DESKTOP VALIDATION**; not merged to `main`; no Step 3.4 / Phase 4.
- 2026-08-09 — **Phase 3 Step 3.2 DESKTOP VALIDATION PASSED + closeout (`feature/wim-servicing` → `main`).** Real Windows 11 25H2 (Chinese Simplified, x64, Consumer Editions, `install.wim`) desktop validation of the offline WIM servicing lifecycle PASSED: ISO inspection succeeded; `Windows 11 专业版` (source index 4) selected → Ready; Prepare exported source index 4 into an isolated single-index working WIM (working index 1) under `%LOCALAPPDATA%\WinForge\Workspaces\<id>\`; the original ISO / original `install.wim` were not modified; Mount succeeded and the real mount directory contained Windows / Program Files / Program Files (x86) / Users / PerfLogs; DISM confirmed a genuine mount; Unmount & discard succeeded and UI returned to Prepared; `dism /English /Get-MountedWimInfo` reported "No mounted images found"; the mount directory was empty after unmount; repeated Mount → Unmount lifecycle passed; the mounted-state source/edition switching safety guard passed; no orphaned active mount remained. 133/133 automated tests pass (Core 21, App 112), 0 errors, 0 warnings (Release). Step 3.2 = **COMPLETED**; Desktop Validation = **PASSED**; Step 3.3 = **NOT STARTED**; no package/component/tweak/customization work implemented.
- 2026-08-09 — **Phase 3 Step 3.2 IMPLEMENTED (`feature/wim-servicing`) — offline WIM servicing lifecycle.** Adds a durable `ImageServicingWorkspace` (state machine `ServicingWorkspaceState`: NotPrepared/Preparing/Prepared/Mounting/Mounted/Unmounting/Completed/Failed + `ServicingHealth`) and Core contract `IImageServicingService` (`PrepareWorkingImageAsync`/`MountAsync`/`UnmountDiscardAsync`/`ValidateServicingWorkspaceAsync`) implemented in Infrastructure via DISM export (`/Export-Image` source index N → standalone WIM index 1), mount (`/Mount-Image`), unmount/discard (`/Unmount-Image /Discard`), and registration verification (`/Get-MountedImageInfo`). The source ISO/install image is NEVER modified — export reads a transient read-only ISO mount and writes a WinForge-owned working copy under `%LOCALAPPDATA%\WinForge\Workspaces\<id>\image\install.wim`; mount uses a dedicated empty `mount\` dir. `IWorkspacePathProvider` addresses workspaces by a safe id segment; `IWorkspaceSafeDelete` proves a target is strictly inside the workspace before any deletion (refuses drive/profile/repo roots). `ImageViewModel` gains prepare/mount/unmount commands with state-aware `Can*` guards and a `BlockedMessage` that refuses ISO re-inspection / edition re-selection while mounted. Post-export validation uses the per-index `/Get-ImageInfo /Index:1` detail query (a real defect was fixed: the original code used the index-less list query, which never reports Architecture/Build, so validation would always have failed). Fixed two compile issues (a bool/result mismatch and three nullable warnings) and added `NullToVisibilityConverter`/`BooleanToFontWeightConverter` so `ImageView.xaml` compiles. 35 new tests (Core 10 + App 25); total **127/127 pass (Core 21, App 106), 0 errors, 0 warnings**. Step 3.2 = **IMPLEMENTED / PENDING USER REAL-DESKTOP VALIDATION**; not merged to `main`; no Step 3.3.
- 2026-08-09 — **Phase 3 Step 3.1 DESKTOP VALIDATION PASSED + elevation closeout (`feature/wim-engine`).** Real Windows 11 25H2 (Chinese Simplified, x64, Consumer Editions, `install.wim`) desktop validation confirmed: ISO inspected, 6 editions enumerated (Windows Version `10.0.26200`, Build `26200`, x64, zh-CN), edition `Windows 11 专业版` (index 4) selected → `ImageWorkspace` status Ready; Source retained as the original `.iso`; UI showed `Image: install.wim` / `Source: <iso>` with no temporary mount drive. Discovered defect: non-elevated launch fails DISM enumeration with **exit code 740** (ERROR_ELEVATION_REQUIRED); elevated launch succeeds. Fix: `app.manifest` changed `asInvoker` → `requireAdministrator` (uiAccess false), wired via `<ApplicationManifest>` in `WinForge.App.csproj` (ADR-018). Regression coverage added: `AppManifestElevationTests` (asserts the source manifest requires `requireAdministrator`) and a Core test confirming `ImageWorkspace.ImageRelativePath` retains the full normalized `sources\install.wim` (UI shows only the filename). 2 new tests (Core 1, App 1); total **92/92 pass (Core 12, App 80), 0 errors, 0 warnings**. Step 3.1 = **COMPLETED**; Step 3.2 NOT STARTED; no WIM mount/export/modification.
- 2026-08-08 — **Phase 3 Step 3.1 — durable WIM workspace & image selection foundation (IMPLEMENTED, `feature/wim-engine`).** Adds a durable `ImageWorkspace` descriptor built from a Phase 2 inspection + selected edition: `SourceIsoPath` (original ISO), `ImageRelativePath` (`sources\install.wim|esd`, derived — never a temp mount drive), `ImageType`, `SelectedIndex`, `SelectedEditionName`, `Architecture`, `Version`, `Build`, `Languages`. Core contracts `IImageWorkspaceFactory` (`BuildWorkspace` → `ImageWorkspaceBuildResult` with `ImageWorkspaceStatus` NotReady/Ready/Invalid + issues) and `IWimService` (read-only `ValidateWorkspace` / `ResolveSelectedImage` → `SelectedImageContext`), both implemented as pure read-only Infrastructure logic (no DISM export/mount/apply/capture; no image modification). `IAppState.CurrentImageWorkspace` holds the durable selection; the Image page builds/updates it on edition select/change and clears it on new-ISO inspection (no stale index). New "Selected image" UI section shows Edition/Index/Image/Architecture/Build/Status/Source. ADR-017 records that durable descriptors never persist temporary mounted drive letters. 30 new tests (Core 5, App 25); total **90/90 pass (Core 11, App 79), 0 errors, 0 warnings**. Step 3.1 = **IMPLEMENTED / PENDING USER DESKTOP VALIDATION**; not merged to `main`, no new tag; Phase 3 remains IN PROGRESS; Step 3.2 NOT STARTED.
- 2026-08-08 — **Phase 2 Step 2.2 formally ACCEPTED and merged to `main`.** Real Windows desktop validation of Windows 11 25H2 (Chinese Simplified, x64, Consumer Editions, `install.wim`) PASSED: ISO mounted, `install.wim` detected, `/Get-ImageInfo` enumeration (6 indexes), per-index detail queries, Windows Version `10.0.26200`, Build `26200`, Architecture `x64`, Language `zh-CN`, localized Chinese edition names, ISO dismounted. Both two-stage real-desktop findings are now fixed and revalidated: (1) initial `/Get-WimInfo` caused DISM exit 87 → corrected to `/Get-ImageInfo`; (2) trailing DISM footer `The operation completed successfully.` was parsed as language `The` → fixed via `TryNormalizeLanguageTag` (UI now shows `zh-CN` only). 60/60 automated tests pass (Core 6, App 54; 0 errors, 0 warnings). Merged via `--no-ff` merge commit into `main`; `v0.1.0-alpha` unchanged; `feature/iso-inspection` retained (history). Step 2.2 = COMPLETED; Phase 2 is formally COMPLETED.
- 2026-08-08 — **Phase 2 Step 2.2 DISM language footer parsing fix (`feature/iso-inspection`, pending final real desktop re-validation).** Real desktop validation of the Windows 11 25H2 zh-CN x64 Consumer `install.wim` SUCCEEDED for the full two-stage `/Get-ImageInfo` flow (6 indexes, version `10.0.26200`, build `26200`, x64, localized Chinese edition names, guaranteed dismount) but exposed a parser defect: `DismImageInfoParser` blindly took the first whitespace token of non-key lines in the `Languages` section, so DISM's footer `The operation completed successfully.` became the language `The` (UI showed `zh-CN, The`). Replaced `ExtractLanguage` with `TryNormalizeLanguageTag` (conservative BCP-47-like validator: accepts `en-US`/`zh-CN`/`pt-BR`/`sr-Latn-RS`, strips `(Default)`, rejects prose) and terminate the `Languages` section on the first non-language, non-blank, non-key line. Added regression tests asserting `Languages == ["zh-CN"]` (not `["zh-CN","The"]`) and `["en-US","fr-CA"]` against the exact real-footer shape, plus rejection of arbitrary prose. Build/test clean (0 errors, 0 warnings, 100% passing). Step 2.2 remains NOT COMPLETED until one final real-desktop re-validation.
- 2026-08-08 — **Phase 2 Step 2.2 DISM Error 87 fix (`feature/iso-inspection`, pending real desktop re-validation).** Real desktop validation exposed DISM exit code 87 because the implementation used `dism.exe /English /Get-WimInfo /ImageFile:"..."` (incorrect for the Windows 11 DISM command line). Corrected to `dism.exe /Get-ImageInfo /ImageFile:"<path>" /English` (enumeration) and `dism.exe /Get-ImageInfo /ImageFile:"<path>" /Index:<n> /English` (per-index detail); `/ImageFile:` kept (not `/WimFile`). Renamed `DismWimInfoParser` → `DismImageInfoParser`; added a regression test asserting production arguments never contain `/Get-WimInfo`. Build/test clean (0 errors, 0 warnings, 100% passing). Step 2.2 remains NOT COMPLETED until real-desktop re-validation.
- 2026-08-08 — **Phase 2 Step 2.2 implemented on `feature/iso-inspection` (pending real ISO desktop re-validation).** Added `IWindowsImageMetadataService` + `WindowsImageMetadataResult` + `WindowsEditionInfo` (extended) in Core; `WindowsImageMetadataService` (DISM `/Get-ImageInfo /ImageFile:... /English`) + pure `DismImageInfoParser` + `IProcessRunner`/`WindowsProcessRunner` in Infrastructure; extended `WindowsIsoInspectionService` into a single mount→layout→metadata→dismount session (ADR-015 preserved). Image page shows Windows information + editions list; edition selection writes `IAppState.SelectedEdition`. 16 new automated tests added (parser, service via fake process runner, orchestrator lifecycle, ViewModel/Home). `dotnet build`/`dotnet test -c Release` verified clean (0 errors, 0 warnings, 100% tests executed and passing). Not merged to `main`, no new tag; Step 2.2 NOT COMPLETED until real-desktop validation.
- 2026-08-08 — **Phase 2 Step 2.1 accepted and merged to `main`.** All Step 2.1 commits from `feature/iso-inspection` (`57c975e`, `66978df`) merged into `main` via a `--no-ff` merge commit; pre-merge and post-merge `dotnet build`/`dotnet test` verified clean (0 errors, 0 warnings, 29/29 tests passing). Real Windows 11 25H2 (zh-CN, x64, Consumer ISO, install.wim) desktop validation completed (mount → inspect → dismount confirmed by user logs). `v0.1.0-alpha` unchanged; Step 2.2 NOT STARTED; `feature/iso-inspection` retained for Step 2.2.
- 2026-08-08 — **Phase 1 merge-readiness fix (logging thread-safety).** `InMemoryLoggerService` now uses a lock-guarded `List<LogEntry>` with a thread-safe snapshot `Entries` and no WPF dependency; `LogsViewModel` marshals background-thread log events to the UI thread via `SynchronizationContext` (ADR-014). Phase 1 remains **COMPLETED**; Phase 2 is **NOT STARTED**. This was a fix only — no Phase 2 functionality was added.
- 2026-08-08 — **Phase 1 merge-readiness fix (read-only WPF binding).** The Image page `TextBox` for `ImageViewModel.FileDisplay` used the default `TwoWay` mode against a getter-only property, throwing at runtime on real Windows desktops. Changed to `Mode=OneWay` (no setter added to `FileDisplay`). Audited all Phase 1 XAML; this was the only default-TwoWay control bound to a read-only property. Added `ImageBindingRegressionTests`. Phase 1 remains **COMPLETED**; Phase 2 is **NOT STARTED**.
- 2026-08-08 — **Phase 1 formally accepted and merged to `main`.** All Phase 1 commits from `feature/application-foundation` (`ac18789`, `89009bb`, `f2f919d`) merged into `main` via a `--no-ff` merge commit; pre-merge and post-merge `dotnet build`/`dotnet test` verified clean (0 errors, 0 warnings, 10/10 tests passing). Annotated tag `v0.1.0-alpha` created and pushed. `feature/application-foundation` retained for history. Phase 1 remains **COMPLETED**; Phase 2 is **NOT STARTED**.
- 2026-08-08 — **Phase 2 — ISO Inspection / Step 2.1 (read-only inspection).** On `feature/iso-inspection`: added Core model/enums (`IsoInspectionResult`, `IsoDetectedType`, `InstallImageType`, `IsoInspectionStatus`) and contracts (`IIsoInspectionService`, `IIsoMountService`); Infrastructure `WindowsIsoInspectionService` (file validation + read-only mount + layout detection, always dismounts) and `WindowsIsoMountService` (`Mount-DiskImage`/`Dismount-DiskImage` via PowerShell, base64-encoded script, safe cleanup). App `IFilePicker`/`WindowsFilePicker`, `ImageViewModel` refactored to `SelectIsoCommand`/`InspectIsoCommand` (async, busy/error states, no UI-thread mount). Added 15 automated tests (9 inspection-logic + 6 ViewModel). Phase 2 is **IN PROGRESS**; not merged, no new tag (v0.1.0-alpha unchanged).
- 2026-08-08 — **Phase 2 Step 2.1 merge-readiness fix (cancellation-safe ISO cleanup).** `WindowsIsoInspectionService` now tracks `mountAttempted` and always attempts a best-effort `Dismount-DiskImage` in the `finally` block using `CancellationToken.None`, so cancellation or a failure that occurs before the mounted root is obtained can never leave an ISO mounted (ADR-015). `OperationCanceledException` is re-thrown rather than swallowed by successful cleanup; other failures surface as a `Failed` result. `WindowsIsoMountService` dismount is safe when the image is not mounted (`-ErrorAction SilentlyContinue`). User-facing `ErrorMessage` is now a generic, friendly string — raw PowerShell/HRESULT/command detail is retained only in `ILoggerService`. Added 4 cancellation/cleanup tests; fixed `ImageBindingRegressionTests` XAML path resolution to be independent of build-output redirection (`[CallerFilePath]`). Phase 2 is **IN PROGRESS**; Step 2.1 remains **pending merge**; no new tag.

### Known Issues
- WIM index / edition / version / architecture / language parsing is implemented and **real-desktop-validated (Step 2.2 COMPLETED)**: a Windows 11 25H2 zh-CN x64 Consumer `install.wim` desktop run validated the two-stage `/Get-ImageInfo` flow (6 indexes, version `10.0.26200`, build `26200`, x64, localized Chinese edition names, guaranteed dismount) and the corrected language parsing (footer `The operation completed successfully.` is now rejected; `Languages == ["zh-CN"]`). ESD (`install.esd`) metadata parsing is implemented but still `Untested` on a real desktop.
- Real Windows 11 25H2 (zh-CN, x64, Consumer ISO, install.wim) desktop validation of the read-only mount/inspect/dismount cycle **was completed** (Step 2.1); the user confirmed the full flow via application logs. Windows **version** compatibility is still not claimed as `Supported` — only ISO-layout `Inspection` is `Tested` for that release (see docs/WINDOWS-COMPATIBILITY.md).

- Build/ISO export is **COMPLETED** (Phase 10 Build Engine, ADR-038…ADR-044), real-desktop validated and merged to `main` on 2026-08-10. The automated suite (440 pass, 0 errors, 0 warnings, Release) is CI-safe and uses fakes; the actual `oscdimg.exe` / DISM ISO-rebuild was exercised on a real Windows 11 25H2 (Chinese Simplified, x64, Consumer Editions, `install.wim`) desktop and PASSED all acceptance points.

### Blocked
- _(none)_

## Verification

| Field | Value |
|-------|-------|
| Last Verified Baseline Commit | e152e6d (Step 3.3 COMPLETED + real-desktop PASSED; Wizard/Localization Phase 3.5 started and completed from here) |
| Last Updated | 2026-08-11 |

> **Baseline commit policy:** `Last Verified Baseline Commit` is the most recent
> confirmed-stable commit *before* the current phase's work, not necessarily the
> current HEAD. It is updated when a phase is accepted, pointing at the stable
> point the phase started from. Do not record a commit's own SHA here.
