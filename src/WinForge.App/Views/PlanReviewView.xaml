<UserControl x:Class="WinForge.App.Views.PlanReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WinForge.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:PlanReviewViewModel}">
    <ScrollViewer Padding="32">
        <StackPanel MaxWidth="900">
            <TextBlock Text="Plan Review" FontSize="24" FontWeight="Bold" Foreground="#1F2933"/>
            <TextBlock Text="Review the declarative plan assembled from the Components, Privacy, and System pages. Execution only begins when you press Apply — never from a checkbox change."
                       FontSize="12" Foreground="#7B8794" Margin="0,4,0,0" TextWrapping="Wrap"/>

            <Border Style="{StaticResource StatusTile}" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="{Binding TotalSelected, StringFormat='Total operations: {0}'}" FontSize="14" FontWeight="SemiBold" Foreground="#1F2933"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding TotalApps, StringFormat='Apps: {0}'}" Margin="0,0,16,0" Foreground="#52606D"/>
                        <TextBlock Text="{Binding TotalPackages, StringFormat='Packages: {0}'}" Margin="0,0,16,0" Foreground="#52606D"/>
                        <TextBlock Text="{Binding TotalRegistry, StringFormat='Registry: {0}'}" Margin="0,0,16,0" Foreground="#52606D"/>
                        <TextBlock Text="{Binding TotalServices, StringFormat='Services: {0}'}" Foreground="#52606D"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <TextBlock Text="Validation warnings" FontSize="16" FontWeight="SemiBold" Foreground="#1F2933" Margin="0,16,0,0"
                       Visibility="{Binding HasWarnings, Converter={StaticResource BoolToVis}}"/>
            <ItemsControl ItemsSource="{Binding Warnings}" Margin="0,8,0,0"
                          Visibility="{Binding HasWarnings, Converter={StaticResource BoolToVis}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="#9B1C1C" FontSize="13" Margin="0,2" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="No validation warnings." Foreground="#2F855A" FontSize="13" Margin="0,8,0,0"
                       Visibility="{Binding HasWarnings, Converter={StaticResource BoolToVisInv}}"/>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Style="{StaticResource PrimaryButton}" Content="Validate plan"
                        Command="{Binding ValidateCommand}" Background="#487EB0"/>
                <Button Style="{StaticResource PrimaryButton}" Content="Apply to mounted image"
                        Command="{Binding ApplyCommand}" Margin="8,0,0,0" Background="#2F855A"/>
            </StackPanel>

            <TextBlock Text="{Binding ProgressText}" FontSize="13" Foreground="#3B82F6" Margin="0,12,0,0" TextWrapping="Wrap"
                       Visibility="{Binding ProgressText, Converter={StaticResource NullToVis}}"/>
            <TextBlock Text="{Binding ResultSummary}" FontSize="13" FontWeight="SemiBold" Foreground="#1F2933" Margin="0,8,0,0" TextWrapping="Wrap"
                       Visibility="{Binding ResultSummary, Converter={StaticResource NullToVis}}"/>

            <TextBlock Text="Note: applying changes the isolated, mounted working image only. The image remains mounted; commit/discard and ISO rebuild happen in later steps."
                       FontSize="12" Foreground="#7B8794" Margin="0,16,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
