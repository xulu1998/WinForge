<UserControl x:Class="WinForge.App.Views.SystemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WinForge.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:SystemViewModel}">
    <ScrollViewer Padding="32">
        <StackPanel MaxWidth="900">
            <TextBlock Text="System" FontSize="24" FontWeight="Bold" Foreground="#1F2933"/>
            <TextBlock Text="Safe, offline service and machine-policy changes only. No boot configuration, partitioning, drivers, kernel patches, or blanket security disabling."
                       FontSize="12" Foreground="#7B8794" Margin="0,4,0,0" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding SelectedTotal, StringFormat='Selected: {0}'}" FontSize="13" FontWeight="SemiBold"
                       Foreground="#1F2933" Margin="0,12,0,0"/>

            <TextBlock Text="Recommended service changes" FontSize="16" FontWeight="SemiBold" Foreground="#1F2933" Margin="0,16,0,0"/>
            <ListView ItemsSource="{Binding RecommendedServices}" BorderBrush="#CBD2D9" BorderThickness="1" Margin="0,8,0,0">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ServiceSelectionItem}">
                        <StackPanel Margin="6">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding Service.DisplayName}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding RecommendedStartType, StringFormat=' → {0}'}" Foreground="#2F855A" FontSize="12" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Service.ServiceName}" FontSize="11" Foreground="#9AA5B1" Margin="28,2,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock Text="System registry tweaks" FontSize="16" FontWeight="SemiBold" Foreground="#1F2933" Margin="0,20,0,0"/>
            <ListView ItemsSource="{Binding RegistrySettings}" BorderBrush="#CBD2D9" BorderThickness="1" Margin="0,8,0,0">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:RegistrySettingItem}">
                        <StackPanel Margin="6">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding Setting.Title}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Setting.Description}" FontSize="12" Foreground="#52606D" Margin="28,2,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</UserControl>
