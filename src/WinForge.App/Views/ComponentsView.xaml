<UserControl x:Class="WinForge.App.Views.ComponentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WinForge.App.ViewModels"
             xmlns:conv="clr-namespace:WinForge.App.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ComponentsViewModel}">
    <ScrollViewer Padding="32">
        <StackPanel MaxWidth="900">
            <TextBlock FontSize="24" FontWeight="Bold" Foreground="#1F2933">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource locKey}">
                        <Binding Source="Components.Title"/><Binding Source="{StaticResource Loc}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <TextBlock FontSize="12" Foreground="#7B8794" Margin="0,4,0,0" TextWrapping="Wrap">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource locKey}">
                        <Binding Source="Components.Intro"/><Binding Source="{StaticResource Loc}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>

            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                <Button Style="{StaticResource PrimaryButton}" Background="#2F855A" Command="{Binding DiscoverCommand}">
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource locKey}">
                                <Binding Source="Customize.Discover"/><Binding Source="{StaticResource Loc}"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Button>
                <TextBlock Foreground="#3B82F6" VerticalAlignment="Center" Margin="12,0,0,0"
                           Visibility="{Binding IsDiscovering, Converter={StaticResource BoolToVis}}">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource locKey}">
                            <Binding Source="Customize.Discovering"/><Binding Source="{StaticResource Loc}"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
            <TextBlock Text="{Binding StatusMessage}" FontSize="13" Foreground="#52606D" Margin="0,8,0,0" TextWrapping="Wrap"/>
            <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#1F2933" Margin="0,4,0,0"
                       Visibility="{Binding HasInventory, Converter={StaticResource BoolToVis}}">
                <Run>
                    <Run.Text>
                        <MultiBinding Converter="{StaticResource locKey}">
                            <Binding Source="Customize.Selected"/><Binding Source="{StaticResource Loc}"/>
                        </MultiBinding>
                    </Run.Text>
                </Run>
                <Run Text=": "/>
                <Run Text="{Binding SelectedTotal}"/>
            </TextBlock>

            <StackPanel Visibility="{Binding HasInventory, Converter={StaticResource BoolToVis}}">
                <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#1F2933" Margin="0,20,0,0">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource locKey}">
                            <Binding Source="Components.ProvisionedApps"/><Binding Source="{StaticResource Loc}"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <ListView ItemsSource="{Binding AppxPackages}" BorderBrush="#CBD2D9" BorderThickness="1" MaxHeight="260" Margin="0,8,0,0">
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:AppxSelectionItem}">
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" IsEnabled="{Binding CanSelect}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding FriendlyName}" Width="300" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding TechnicalId}" Foreground="#9AA5B1" FontSize="11" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Reason}" Foreground="#9B1C1C" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#1F2933" Margin="0,20,0,0">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource locKey}">
                            <Binding Source="Components.WindowsPackages"/><Binding Source="{StaticResource Loc}"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock FontSize="12" Foreground="#7B8794" Margin="0,4,0,0" TextWrapping="Wrap">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource locKey}">
                            <Binding Source="Components.PackagesHint"/><Binding Source="{StaticResource Loc}"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <ListView ItemsSource="{Binding WindowsPackages}" BorderBrush="#CBD2D9" BorderThickness="1" MaxHeight="200" Margin="0,8,0,0">
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:PackageSelectionItem}">
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" IsEnabled="{Binding CanSelect}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding FriendlyName}" Width="320" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding TechnicalId}" Foreground="#9AA5B1" FontSize="11" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Reason}" Foreground="#9B1C1C" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="Wrap"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#1F2933" Margin="0,20,0,0">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource locKey}">
                            <Binding Source="Components.Services"/><Binding Source="{StaticResource Loc}"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock FontSize="12" Foreground="#7B8794" Margin="0,4,0,0" TextWrapping="Wrap">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource locKey}">
                            <Binding Source="Components.ServicesHint"/><Binding Source="{StaticResource Loc}"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <ListView ItemsSource="{Binding Services}" BorderBrush="#CBD2D9" BorderThickness="1" MaxHeight="220" Margin="0,8,0,0">
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ServiceSelectionItem}">
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" IsEnabled="{Binding CanSelect}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding FriendlyName}" Width="320" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding RecommendedStartType, StringFormat='→ {0}'}" Foreground="#9AA5B1" FontSize="11" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding TechnicalId}" Foreground="#9AA5B1" FontSize="11" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Reason}" Foreground="#9B1C1C" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
