<UserControl x:Class="WinForge.App.Views.PrivacyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WinForge.App.ViewModels"
             xmlns:conv="clr-namespace:WinForge.App.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:PrivacyViewModel}">
    <ScrollViewer Padding="32">
        <StackPanel MaxWidth="900">
            <TextBlock FontSize="24" FontWeight="Bold" Foreground="#1F2933">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource locKey}">
                        <Binding Source="Privacy.Title"/><Binding Source="{StaticResource Loc}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <TextBlock FontSize="12" Foreground="#7B8794" Margin="0,4,0,0" TextWrapping="Wrap">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource locKey}">
                        <Binding Source="Privacy.Intro"/><Binding Source="{StaticResource Loc}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#1F2933" Margin="0,12,0,0">
                <Run>
                    <Run.Text>
                        <MultiBinding Converter="{StaticResource locKey}">
                            <Binding Source="Privacy.Selected"/><Binding Source="{StaticResource Loc}"/>
                        </MultiBinding>
                    </Run.Text>
                </Run>
                <Run Text=": "/>
                <Run Text="{Binding SelectedTotal}"/>
            </TextBlock>

            <ListView ItemsSource="{Binding Settings}" BorderBrush="#CBD2D9" BorderThickness="1" Margin="0,8,0,0">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:RegistrySettingItem}">
                        <StackPanel Margin="6">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding Setting.Title}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Setting.Description}" FontSize="12" Foreground="#52606D" Margin="28,2,0,0" TextWrapping="Wrap"/>
                            <TextBlock FontSize="11" Foreground="#9AA5B1" Margin="28,2,0,0" TextWrapping="Wrap">
                                <Run Text="{Binding Setting.Hive}"/><Run Text="\"/><Run Text="{Binding Setting.KeyPath}"/><Run Text="\"/><Run Text="{Binding Setting.ValueName}"/>
                                <Run Text=" = "/><Run Text="{Binding Setting.RecommendedData}"/>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</UserControl>
