<UserControl x:Class="WinForge.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WinForge.App.ViewModels"
             xmlns:views="clr-namespace:WinForge.App.Views"
             xmlns:conv="clr-namespace:WinForge.App.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:SettingsViewModel}">
    <ScrollViewer Padding="32">
        <StackPanel MaxWidth="820">
            <TextBlock FontSize="24" FontWeight="Bold" Foreground="#1F2933">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource locKey}">
                        <Binding Source="Settings.Title"/><Binding Source="{StaticResource Loc}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>

            <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#1F2933" Margin="0,24,0,0">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource locKey}">
                        <Binding Source="Settings.Language"/><Binding Source="{StaticResource Loc}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>

            <StackPanel Margin="0,8,0,0">
                <RadioButton GroupName="Language" Margin="0,6,0,0"
                             IsChecked="{Binding IsEnglish, Mode=OneWay}"
                             Command="{Binding SetLanguageCommand}" CommandParameter="en">
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource locKey}">
                                <Binding Source="Settings.Language.English"/><Binding Source="{StaticResource Loc}"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </RadioButton>
                <RadioButton GroupName="Language" Margin="0,6,0,0"
                             IsChecked="{Binding IsChinese, Mode=OneWay}"
                             Command="{Binding SetLanguageCommand}" CommandParameter="zh-CN">
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource locKey}">
                                <Binding Source="Settings.Language.Chinese"/><Binding Source="{StaticResource Loc}"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </RadioButton>
            </StackPanel>

            <TextBlock FontSize="12" Foreground="#7B8794" Margin="0,12,0,0" TextWrapping="Wrap"
                       MaxWidth="820">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource locKey}">
                        <Binding Source="Settings.Language.Note"/><Binding Source="{StaticResource Loc}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>

            <views:StorageView DataContext="{Binding Storage, Mode=OneWay}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
